# 功能树模型 + 决策导向报告结构 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把功能树从一维清单升级为「加权三层 + 确定性派生引擎」，并把报告物理重排为决策四层（事实→比较→洞察→决策）+ 决策摘要。

**Architecture:** 完全对偶现有定价模型——LLM 只填稀疏 schema（功能点支持度/深度/证据等级），新增 `src/feature_model.py` 纯函数引擎做确定性派生（覆盖率/winner/差异点/护城河/蓝海），同输入恒同输出、可溯源。analyzer 接引擎（仿 `_apply_pricing_engine`），writer 物理重排章节，reviewer 加抗假精确校验。

**Tech Stack:** Python 3.14（scoop）、unittest/pytest、PyYAML、LangGraph（已有，不动控制流）。

## Global Constraints

- **测试解释器**：scoop 的 Python 3.14。**不要**用 PATH/conda 的 python（见用户 memory `python-interpreter`）。下文 `python` 一律指该解释器。
- **源码为扁平单文件模块** `src/*.py`，非子包。新模块直接放 `src/`。
- **evidence_id 格式**：`"S" + sha1(...).hexdigest()[:7].upper()` = 8 字符。引擎/校验中凡校验 chip，正则用 `S[0-9A-F]{7}`。
- **抑制假精确（铁律）**：无证据 → `null`/`unknown`/`?`，绝不编分；引擎缺数据返回 `None`/空列表，绝不估算。
- **样本内差异点**：措辞强制带「样本内 N 个产品中」，禁止「独占」。
- **support_status 规范值**：`supported | partial | unsupported | unknown`。引擎须同时兼容现有产出的 `partially_supported`/`not_supported`（归一化映射）。
- **depth_score**：`1..5 | None`。仅当存在 官方说明/实测/第三方评测/用户案例 之一才给分，否则 `None`。
- **commit 信息结尾**附：`Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`。提交后自动 `git push`（见 memory `auto-push-after-commit`）。
- **不动控制流**：直线 Collector→Analyzer→Writer→Reviewer→guard_revise→END 不变。

## 文件结构（创建/修改 + 职责）

**新建：**
- `src/feature_model.py` —— 功能树确定性派生引擎（纯函数，对偶 `pricing_model.py`）。
- `src/feature_weights.py` —— `config/feature_weights.yaml` 加载器（对偶 `scoring_config.py`）。
- `config/feature_weights.yaml` —— 能力域权重 × analysis_focus，带 version。
- `tests/test_feature_model.py` —— 引擎单测（覆盖率 unknown 规则 / winner 保守 / 护城河 / 蓝海 / 差异点）。
- `tests/test_feature_weights.py` —— 加载器单测（缺失回退 / focus 选择 / version）。
- `tests/test_feature_tree_assembly.py` —— analyzer 侧确定性装配单测（skeleton / 归一化 / 引擎挂载）。
- `tests/test_writer_decision_report.py` —— writer 四层结构 + §0/§5/§10/§13 golden 测试。
- `tests/test_reviewer_feature_checks.py` —— reviewer 新校验单测。

**修改：**
- `src/analyzer.py` —— 功能点 fill 产出新叶子字段；新增 `_apply_feature_engine`；功能树 skeleton 用 domains；§3 定价 facts 去 archetype 输出。
- `src/writer.py` —— `writer_node` 四层重排 + 新增 `_render_decision_summary`/`_render_tech_capability`/`_render_business_model`/`_render_caliber_lock`；改写 `_render_feature_gaps`/`_render_pricing`（含顶表 bug 修复）。
- `src/reviewer.py` —— 新增 `check_feature_depth_honesty` / `check_decision_summary_confidence` / `check_differentiator_wording`，注册进 `RULE_RUNNERS` + `MODE_CONFIG`。
- `src/state.py` —— `analysis_meta` 增 `feature_weight_version`（可选，注释说明）。

---

# Phase 1 · 功能树引擎 + 权重配置（纯确定性，零 LLM）

> 本阶段全部可独立测试，是整个目标的地基。完成后引擎能对一棵手写功能树算出全部派生指标。

### Task 1: 权重配置加载器 `feature_weights.py` + yaml

**Files:**
- Create: `config/feature_weights.yaml`
- Create: `src/feature_weights.py`
- Test: `tests/test_feature_weights.py`

**Interfaces:**
- Produces:
  - `reload(path: Optional[Path]=None) -> dict`
  - `version() -> str`（缺失回退 `"unversioned"`）
  - `domains_for_focus(focus: str) -> list[dict]`（每项 `{id,name,weight,role}`；focus 未命中回退 `default_focus`；全缺回退内置默认两域，保证非空）

- [ ] **Step 1: 写 yaml**

```yaml
# config/feature_weights.yaml — 能力域权重 × analysis_focus（带 version，可复现）
version: demo-v1
default_focus: "视频生成质量与可控性"
focuses:
  "视频生成质量与可控性":
    - {id: A, name: 视频生成能力, weight: 0.30, role: core}
    - {id: B, name: 可控性, weight: 0.25, role: core}
    - {id: C, name: 编辑与后处理, weight: 0.15, role: core}
    - {id: D, name: 模板与素材生态, weight: 0.15, role: ecosystem}
    - {id: E, name: 商业化与协作导出, weight: 0.15, role: commercial}
  "小红书内容生产效率":
    - {id: A, name: 模板与素材生态, weight: 0.30, role: ecosystem}
    - {id: B, name: 一键成片与批量, weight: 0.25, role: core}
    - {id: C, name: 文案与脚本辅助, weight: 0.20, role: core}
    - {id: D, name: 视频质量, weight: 0.15, role: core}
    - {id: E, name: 商业化与发布链路, weight: 0.10, role: commercial}
```

- [ ] **Step 2: 写 failing 测试**

```python
# tests/test_feature_weights.py
import unittest
from pathlib import Path
import tempfile, textwrap
from src import feature_weights as fw


class FeatureWeightsTest(unittest.TestCase):
    def setUp(self):
        fw.reload()  # 默认 config/feature_weights.yaml

    def test_version(self):
        self.assertEqual(fw.version(), "demo-v1")

    def test_domains_sum_to_one(self):
        doms = fw.domains_for_focus("视频生成质量与可控性")
        self.assertEqual(len(doms), 5)
        self.assertAlmostEqual(sum(d["weight"] for d in doms), 1.0, places=6)

    def test_unknown_focus_falls_back_to_default(self):
        doms = fw.domains_for_focus("不存在的焦点")
        self.assertEqual([d["id"] for d in doms],
                         [d["id"] for d in fw.domains_for_focus("视频生成质量与可控性")])

    def test_missing_file_falls_back_to_nonempty_default(self):
        fw.reload(Path(tempfile.gettempdir()) / "no_such_feature_weights.yaml")
        doms = fw.domains_for_focus("任意")
        self.assertGreaterEqual(len(doms), 2)
        self.assertEqual(fw.version(), "unversioned")
        fw.reload()
```

- [ ] **Step 3: 跑测试确认 fail**

Run: `python -m pytest tests/test_feature_weights.py -v`
Expected: FAIL（`No module named src.feature_weights`）

- [ ] **Step 4: 写实现**

```python
# src/feature_weights.py
"""能力域权重加载器 — 读 config/feature_weights.yaml。

对偶 scoring_config.py:任何缺失回退内置默认,绝不 break。权重随 analysis_focus
重排是功能树有决策价值的前提;落 config 版本号保证可复现。
"""
from __future__ import annotations

from pathlib import Path
from typing import Optional

try:
    import yaml
except Exception:  # noqa: BLE001
    yaml = None

_DEFAULT_PATH = Path(__file__).resolve().parent.parent / "config" / "feature_weights.yaml"
_CFG: Optional[dict] = None

# 内置兜底:yaml/文件缺失时仍给一组非空权重(和为 1),保证 skeleton 不塌
_FALLBACK = [
    {"id": "A", "name": "核心能力", "weight": 0.5, "role": "core"},
    {"id": "B", "name": "辅助能力", "weight": 0.5, "role": "core"},
]


def reload(path: Optional[Path] = None) -> dict:
    global _CFG
    p = Path(path) if path else _DEFAULT_PATH
    if yaml is None or not p.exists():
        _CFG = {}
        return _CFG
    try:
        _CFG = yaml.safe_load(p.read_text(encoding="utf-8")) or {}
    except Exception:  # noqa: BLE001
        _CFG = {}
    return _CFG


def _cfg() -> dict:
    global _CFG
    if _CFG is None:
        reload()
    return _CFG or {}


def version() -> str:
    v = _cfg().get("version")
    return str(v) if v else "unversioned"


def _normalize(doms: list[dict]) -> list[dict]:
    out = []
    for d in doms or []:
        if not isinstance(d, dict) or "weight" not in d:
            continue
        out.append({
            "id": str(d.get("id", "")),
            "name": str(d.get("name", "")),
            "weight": float(d["weight"]),
            "role": str(d.get("role", "core")),
        })
    return out


def domains_for_focus(focus: str) -> list[dict]:
    cfg = _cfg()
    focuses = cfg.get("focuses") or {}
    doms = _normalize(focuses.get(focus) or [])
    if doms:
        return doms
    default_key = cfg.get("default_focus")
    doms = _normalize(focuses.get(default_key) or [])
    if doms:
        return doms
    return [dict(d) for d in _FALLBACK]
```

- [ ] **Step 5: 跑测试确认 pass**

Run: `python -m pytest tests/test_feature_weights.py -v`
Expected: PASS (4 passed)

- [ ] **Step 6: commit**

```bash
git add config/feature_weights.yaml src/feature_weights.py tests/test_feature_weights.py
git commit -m "feat(feature): 能力域权重配置 + 加载器(对偶 scoring_config)"
```

---

### Task 2: 引擎叶子级原子函数（support/depth/evidence 归一化）

**Files:**
- Create: `src/feature_model.py`
- Test: `tests/test_feature_model.py`

**Interfaces:**
- Produces:
  - `normalize_status(status: str) -> str`（`partially_supported→partial`, `not_supported→unsupported`，未知值→`unknown`）
  - `support_score(status: str) -> Optional[float]`（supported=1.0, partial=0.5, unsupported=0.0, unknown→None）
  - `depth_norm(depth_score: Optional[int]) -> Optional[float]`（1..5→/5；None/越界→None）
  - `evidence_level_score(level: str) -> float`（official=1.0, third_party=0.8, user_review=0.6, inferred=0.3, 其它=0.3）

- [ ] **Step 1: 写 failing 测试**

```python
# tests/test_feature_model.py
import unittest
from src.feature_model import (
    normalize_status, support_score, depth_norm, evidence_level_score,
)


class AtomTest(unittest.TestCase):
    def test_normalize_legacy_status(self):
        self.assertEqual(normalize_status("partially_supported"), "partial")
        self.assertEqual(normalize_status("not_supported"), "unsupported")
        self.assertEqual(normalize_status("supported"), "supported")
        self.assertEqual(normalize_status("garbage"), "unknown")

    def test_support_score_excludes_unknown(self):
        self.assertEqual(support_score("supported"), 1.0)
        self.assertEqual(support_score("partial"), 0.5)
        self.assertEqual(support_score("unsupported"), 0.0)
        self.assertIsNone(support_score("unknown"))

    def test_depth_norm(self):
        self.assertAlmostEqual(depth_norm(4), 0.8)
        self.assertIsNone(depth_norm(None))
        self.assertIsNone(depth_norm(0))
        self.assertIsNone(depth_norm(6))

    def test_evidence_level_score(self):
        self.assertEqual(evidence_level_score("official"), 1.0)
        self.assertEqual(evidence_level_score("third_party"), 0.8)
        self.assertEqual(evidence_level_score("user_review"), 0.6)
        self.assertEqual(evidence_level_score("inferred"), 0.3)
        self.assertEqual(evidence_level_score("anything_else"), 0.3)
```

- [ ] **Step 2: 跑测试确认 fail**

Run: `python -m pytest tests/test_feature_model.py -v`
Expected: FAIL（`No module named src.feature_model`）

- [ ] **Step 3: 写实现（文件头 + 原子函数）**

```python
# src/feature_model.py
"""确定性功能树派生引擎 —— 把抽取出的功能点事实算成可比指标。

对偶 pricing_model.py:LLM 只把各产品功能「翻译」进统一叶子 schema(填空,不算术),
本模块对该 schema 做确定性派生(加权覆盖率 / winner / 样本内差异点 / 护城河 /
蓝海),每个结论可溯源、同输入恒同输出。

铁律:
  - depth_score 缺失 → None(渲染为 ?),绝不编分。
  - 全员缺 depth_score → winner 不允许判产品,只能 tie/unclear。
  - 覆盖率:unknown 既不进分子也不进分母(单独进 evidence_coverage_rate)。
  - 样本内差异点措辞带「样本内 N 个产品中」,禁止「独占」。
"""
from __future__ import annotations

from typing import Optional

_STATUS_ALIAS = {
    "partially_supported": "partial",
    "not_supported": "unsupported",
}
_VALID_STATUS = {"supported", "partial", "unsupported", "unknown"}
_SUPPORT_SCORE = {"supported": 1.0, "partial": 0.5, "unsupported": 0.0}
_EVIDENCE_SCORE = {"official": 1.0, "third_party": 0.8, "user_review": 0.6, "inferred": 0.3}


def normalize_status(status: str) -> str:
    s = _STATUS_ALIAS.get(status, status)
    return s if s in _VALID_STATUS else "unknown"


def support_score(status: str) -> Optional[float]:
    """supported=1 / partial=0.5 / unsupported=0 / unknown→None(排除出覆盖率)。"""
    return _SUPPORT_SCORE.get(normalize_status(status))


def depth_norm(depth_score: Optional[int]) -> Optional[float]:
    """1..5 → 0.2..1.0;None/越界 → None(诚实缺失)。"""
    if depth_score is None:
        return None
    try:
        d = int(depth_score)
    except (TypeError, ValueError):
        return None
    if d < 1 or d > 5:
        return None
    return round(d / 5, 4)


def evidence_level_score(level: str) -> float:
    return _EVIDENCE_SCORE.get(level, 0.3)
```

- [ ] **Step 4: 跑测试确认 pass**

Run: `python -m pytest tests/test_feature_model.py -v`
Expected: PASS (4 passed)

- [ ] **Step 5: commit**

```bash
git add src/feature_model.py tests/test_feature_model.py
git commit -m "feat(feature): 引擎叶子级原子函数 + 状态归一化"
```

---

### Task 3: 加权覆盖率（unknown 排除规则）

**Files:**
- Modify: `src/feature_model.py`
- Test: `tests/test_feature_model.py`

**Interfaces:**
- Consumes: `support_score`（Task 2）
- Produces:
  - `_leaves_of_domain(domain: dict) -> list[dict]`（展平 modules→points，无 modules 时读 domain["points"]）
  - `domain_coverage(domain: dict, product: str) -> dict` → `{"score": float|None, "evidence_rate": float, "known": int, "total": int}`
  - `weighted_coverage(tree: dict, product: str) -> dict` → `{"coverage_known_only": float|None, "evidence_coverage_rate": float, "by_domain": list}`
- **Tree schema 约定**：`tree = {"feature_weight_version": str, "domains": [{"id","name","weight","role","modules":[{"id","name","points":[leaf,...]}]}]}`；leaf = `{"id","name","products":{<P>:{support_status,depth_score,evidence_level,differentiator,source_refs,moat_factors?}}}`。

- [ ] **Step 1: 写 failing 测试**

```python
# 追加到 tests/test_feature_model.py
from src.feature_model import domain_coverage, weighted_coverage


def _leaf(name, **pdata):
    return {"id": name, "name": name, "products": pdata}


def _p(status, depth=None, lvl="official", diff=False, refs=("S1234567",)):
    return {"support_status": status, "depth_score": depth, "evidence_level": lvl,
            "differentiator": diff, "source_refs": list(refs)}


TREE = {
    "feature_weight_version": "demo-v1",
    "domains": [
        {"id": "A", "name": "视频生成", "weight": 0.6, "role": "core", "modules": [
            {"id": "A1", "name": "生成", "points": [
                _leaf("文生视频", Jimeng=_p("supported", 3), Kling=_p("supported", 4)),
                _leaf("首尾帧", Jimeng=_p("unknown"), Kling=_p("supported", 4)),
            ]},
        ]},
        {"id": "B", "name": "可控性", "weight": 0.4, "role": "core", "modules": [
            {"id": "B1", "name": "运镜", "points": [
                _leaf("运镜控制", Jimeng=_p("partial", 2), Kling=_p("supported", 3)),
            ]},
        ]},
    ],
}


class CoverageTest(unittest.TestCase):
    def test_domain_coverage_excludes_unknown(self):
        # 域A Jimeng:文生视频 supported(1),首尾帧 unknown(排除) → score=1.0,evidence_rate=1/2
        cov = domain_coverage(TREE["domains"][0], "Jimeng")
        self.assertAlmostEqual(cov["score"], 1.0)
        self.assertAlmostEqual(cov["evidence_rate"], 0.5)
        self.assertEqual((cov["known"], cov["total"]), (1, 2))

    def test_all_unknown_domain_score_is_none(self):
        d = {"id": "Z", "name": "z", "weight": 1.0, "modules": [
            {"id": "Z1", "name": "z", "points": [_leaf("x", Jimeng=_p("unknown"))]}]}
        cov = domain_coverage(d, "Jimeng")
        self.assertIsNone(cov["score"])
        self.assertEqual(cov["evidence_rate"], 0.0)

    def test_weighted_coverage_known_only(self):
        # Jimeng: 域A score=1.0(w0.6), 域B score=partial0.5(w0.4)
        #   known_only = (0.6*1.0 + 0.4*0.5)/(0.6+0.4) = 0.8
        out = weighted_coverage(TREE, "Jimeng")
        self.assertAlmostEqual(out["coverage_known_only"], 0.8, places=4)
        # evidence: 域A 1/2 项有据(w0.6), 域B 1/1(w0.4) → (0.6*0.5 + 0.4*1.0)/1.0 = 0.7
        self.assertAlmostEqual(out["evidence_coverage_rate"], 0.7, places=4)

    def test_unknown_domain_excluded_from_known_only_denominator(self):
        tree2 = {"domains": [
            {"id": "A", "name": "a", "weight": 0.5, "modules": [
                {"id": "A1", "name": "a", "points": [_leaf("x", P=_p("supported", 3))]}]},
            {"id": "B", "name": "b", "weight": 0.5, "modules": [
                {"id": "B1", "name": "b", "points": [_leaf("y", P=_p("unknown"))]}]},
        ]}
        out = weighted_coverage(tree2, "P")
        # 域B 全 unknown → 不进 known_only 分母 → coverage=1.0; evidence=(0.5*1+0.5*0)/1=0.5
        self.assertAlmostEqual(out["coverage_known_only"], 1.0, places=4)
        self.assertAlmostEqual(out["evidence_coverage_rate"], 0.5, places=4)
```

- [ ] **Step 2: 跑测试确认 fail**

Run: `python -m pytest tests/test_feature_model.py::CoverageTest -v`
Expected: FAIL（`cannot import name 'domain_coverage'`）

- [ ] **Step 3: 写实现（追加到 `src/feature_model.py`）**

```python
def _leaves_of_domain(domain: dict) -> list[dict]:
    """展平 modules→points;无 modules 时退回 domain['points']。"""
    if domain.get("modules"):
        out = []
        for m in domain["modules"]:
            out.extend(m.get("points") or [])
        return out
    return domain.get("points") or []


def domain_coverage(domain: dict, product: str) -> dict:
    leaves = _leaves_of_domain(domain)
    total = len(leaves)
    scores = []
    for leaf in leaves:
        pdata = (leaf.get("products") or {}).get(product) or {}
        s = support_score(pdata.get("support_status", "unknown"))
        if s is not None:
            scores.append(s)
    known = len(scores)
    return {
        "score": round(sum(scores) / known, 4) if known else None,
        "evidence_rate": round(known / total, 4) if total else 0.0,
        "known": known,
        "total": total,
    }


def weighted_coverage(tree: dict, product: str) -> dict:
    """加权覆盖率。unknown 不进 known_only 的分子/分母,单独进 evidence_coverage_rate。"""
    by_domain = []
    num_known = den_known = 0.0
    num_evi = den_evi = 0.0
    for domain in tree.get("domains") or []:
        w = float(domain.get("weight", 0.0))
        cov = domain_coverage(domain, product)
        by_domain.append({"id": domain.get("id"), "name": domain.get("name"),
                          "weight": w, **cov})
        den_evi += w
        num_evi += w * cov["evidence_rate"]
        if cov["score"] is not None:        # 全 unknown 的域整域排除出 known_only
            den_known += w
            num_known += w * cov["score"]
    return {
        "coverage_known_only": round(num_known / den_known, 4) if den_known else None,
        "evidence_coverage_rate": round(num_evi / den_evi, 4) if den_evi else 0.0,
        "by_domain": by_domain,
    }
```

- [ ] **Step 4: 跑测试确认 pass**

Run: `python -m pytest tests/test_feature_model.py::CoverageTest -v`
Expected: PASS (4 passed)

- [ ] **Step 5: commit**

```bash
git add src/feature_model.py tests/test_feature_model.py
git commit -m "feat(feature): 加权覆盖率(unknown 排除规则 + evidence_coverage_rate)"
```

---

### Task 4: feature_winner（保守口径，缺深度→tie/unclear）

**Files:**
- Modify: `src/feature_model.py`
- Test: `tests/test_feature_model.py`

**Interfaces:**
- Consumes: `support_score` / `depth_norm` / `evidence_level_score`
- Produces:
  - `_leaf_product_score(pdata: dict) -> Optional[float]`（unknown→None；加权 support/depth/evidence + differentiator bonus）
  - `feature_winner(point: dict, products: list[str]) -> dict` → `{"winner": str, "reason": str, "confidence": "high|medium|low"}`，winner ∈ 产品名 | `"tie"` | `"unclear"`
- 模块常量：`_WINNER_W = {"support":0.35,"depth":0.40,"evidence":0.15}`，`_DIFF_BONUS = 0.10`，`_TIE_EPS = 0.05`，`_HIGH_MARGIN = 0.15`

- [ ] **Step 1: 写 failing 测试**

```python
# 追加到 tests/test_feature_model.py
from src.feature_model import feature_winner


class WinnerTest(unittest.TestCase):
    def test_clear_winner_with_depth(self):
        pt = _leaf("运镜", Jimeng=_p("partial", 2), Runway=_p("supported", 5))
        out = feature_winner(pt, ["Jimeng", "Runway"])
        self.assertEqual(out["winner"], "Runway")
        self.assertEqual(out["confidence"], "high")

    def test_no_depth_anywhere_forces_tie(self):
        # 全靠 support_status,无任何 depth → 不允许强判
        pt = _leaf("文生视频", Jimeng=_p("supported", None), Kling=_p("supported", None))
        out = feature_winner(pt, ["Jimeng", "Kling"])
        self.assertIn(out["winner"], ("tie", "unclear"))
        self.assertEqual(out["confidence"], "low")

    def test_all_unknown_is_unclear(self):
        pt = _leaf("x", Jimeng=_p("unknown"), Kling=_p("unknown"))
        out = feature_winner(pt, ["Jimeng", "Kling"])
        self.assertEqual(out["winner"], "unclear")

    def test_close_scores_is_tie(self):
        pt = _leaf("x", Jimeng=_p("supported", 4), Kling=_p("supported", 4))
        out = feature_winner(pt, ["Jimeng", "Kling"])
        self.assertEqual(out["winner"], "tie")
```

- [ ] **Step 2: 跑测试确认 fail**

Run: `python -m pytest tests/test_feature_model.py::WinnerTest -v`
Expected: FAIL（`cannot import name 'feature_winner'`）

- [ ] **Step 3: 写实现（追加到 `src/feature_model.py`）**

```python
_WINNER_W = {"support": 0.35, "depth": 0.40, "evidence": 0.15}
_DIFF_BONUS = 0.10
_TIE_EPS = 0.05
_HIGH_MARGIN = 0.15


def _leaf_product_score(pdata: dict) -> Optional[float]:
    s = support_score(pdata.get("support_status", "unknown"))
    if s is None:                      # unknown 不参与比较
        return None
    d = depth_norm(pdata.get("depth_score"))
    e = evidence_level_score(pdata.get("evidence_level", "inferred"))
    bonus = _DIFF_BONUS if pdata.get("differentiator") else 0.0
    return (_WINNER_W["support"] * s
            + _WINNER_W["depth"] * (d if d is not None else 0.0)
            + _WINNER_W["evidence"] * e
            + bonus)


def feature_winner(point: dict, products: list[str]) -> dict:
    prods = (point.get("products") or {})
    scored, any_depth = {}, False
    for p in products:
        pdata = prods.get(p) or {}
        sc = _leaf_product_score(pdata)
        if sc is None:
            continue
        scored[p] = sc
        if depth_norm(pdata.get("depth_score")) is not None:
            any_depth = True
    if not scored:
        return {"winner": "unclear", "reason": "所有产品该能力均无证据(unknown)", "confidence": "low"}
    if not any_depth:
        return {"winner": "tie",
                "reason": "仅有支持度证据、无任何深度评分,不足以强判优劣",
                "confidence": "low"}
    ranked = sorted(scored.items(), key=lambda kv: kv[1], reverse=True)
    top_p, top_s = ranked[0]
    second_s = ranked[1][1] if len(ranked) > 1 else 0.0
    margin = top_s - second_s
    if margin < _TIE_EPS:
        return {"winner": "tie", "reason": f"{top_p} 与次优分差 {margin:.2f}<{_TIE_EPS},判平",
                "confidence": "medium"}
    confidence = "high" if margin >= _HIGH_MARGIN else "medium"
    return {"winner": top_p, "reason": f"{top_p} 综合分领先次优 {margin:.2f}",
            "confidence": confidence}
```

- [ ] **Step 4: 跑测试确认 pass**

Run: `python -m pytest tests/test_feature_model.py::WinnerTest -v`
Expected: PASS (4 passed)

- [ ] **Step 5: commit**

```bash
git add src/feature_model.py tests/test_feature_model.py
git commit -m "feat(feature): feature_winner 保守口径(缺深度→tie/unclear)"
```

---

### Task 5: 样本内差异点矩阵 + product_archetype

**Files:**
- Modify: `src/feature_model.py`
- Test: `tests/test_feature_model.py`

**Interfaces:**
- Consumes: `support_score` / `domain_coverage` / `_leaves_of_domain`
- Produces:
  - `differentiation_matrix(tree: dict, products: list[str]) -> list[dict]` → 每项 `{"feature_id","name","product","note"}`，note 含「样本内 N 个产品中」
  - `product_archetype(tree: dict, product: str) -> str` ∈ `全能型|专精型|工具型|平台型|数据不足`
- 模块常量：`_STRONG = 0.7`，`_BREADTH_BROAD = 0.8`

- [ ] **Step 1: 写 failing 测试**

```python
# 追加到 tests/test_feature_model.py
from src.feature_model import differentiation_matrix, product_archetype


class DiffMatrixTest(unittest.TestCase):
    def test_only_one_product_supported_is_differentiator(self):
        tree = {"domains": [{"id": "A", "name": "a", "weight": 1.0, "modules": [
            {"id": "A1", "name": "a", "points": [
                _leaf("多镜头", Jimeng=_p("unsupported"), Kling=_p("partial"),
                      Runway=_p("supported", 4, diff=True)),
            ]}]}]}
        rows = differentiation_matrix(tree, ["Jimeng", "Kling", "Runway"])
        self.assertEqual(len(rows), 1)
        self.assertEqual(rows[0]["product"], "Runway")
        self.assertIn("样本内 3 个产品中", rows[0]["note"])
        self.assertNotIn("独占", rows[0]["note"])


class ArchetypeTest(unittest.TestCase):
    def test_broad_and_strong_is_allrounder(self):
        tree = {"domains": [
            {"id": "A", "name": "a", "weight": 0.5, "modules": [{"id": "A1", "name": "a",
                "points": [_leaf("x", P=_p("supported", 5))]}]},
            {"id": "B", "name": "b", "weight": 0.5, "modules": [{"id": "B1", "name": "b",
                "points": [_leaf("y", P=_p("supported", 4))]}]},
        ]}
        self.assertEqual(product_archetype(tree, "P"), "全能型")

    def test_narrow_strong_is_specialist(self):
        tree = {"domains": [
            {"id": "A", "name": "a", "weight": 0.5, "role": "core", "modules": [{"id": "A1",
                "name": "a", "points": [_leaf("x", P=_p("supported", 5))]}]},
            {"id": "B", "name": "b", "weight": 0.5, "role": "core", "modules": [{"id": "B1",
                "name": "b", "points": [_leaf("y", P=_p("unsupported"))]}]},
        ]}
        self.assertEqual(product_archetype(tree, "P"), "专精型")

    def test_all_unknown_is_insufficient(self):
        tree = {"domains": [{"id": "A", "name": "a", "weight": 1.0, "modules": [{"id": "A1",
            "name": "a", "points": [_leaf("x", P=_p("unknown"))]}]}]}
        self.assertEqual(product_archetype(tree, "P"), "数据不足")
```

- [ ] **Step 2: 跑测试确认 fail**

Run: `python -m pytest tests/test_feature_model.py::DiffMatrixTest tests/test_feature_model.py::ArchetypeTest -v`
Expected: FAIL（`cannot import name 'differentiation_matrix'`）

- [ ] **Step 3: 写实现（追加到 `src/feature_model.py`）**

```python
_STRONG = 0.7
_BREADTH_BROAD = 0.8


def differentiation_matrix(tree: dict, products: list[str]) -> list[dict]:
    """样本内差异点:某能力上仅一个产品做到位(differentiator 或独家 supported)。
    措辞强制带「样本内 N 个产品中」,不绝对化为「独占」。"""
    n = len(products)
    rows = []
    for domain in tree.get("domains") or []:
        for leaf in _leaves_of_domain(domain):
            prods = leaf.get("products") or {}
            flagged = [p for p in products if (prods.get(p) or {}).get("differentiator")]
            if len(flagged) != 1:
                # 退一步:仅一个产品 supported、其余非 supported,也算样本内差异点
                supported = [p for p in products
                             if support_score((prods.get(p) or {}).get("support_status", "unknown")) == 1.0]
                if len(supported) != 1:
                    continue
                flagged = supported
            p = flagged[0]
            rows.append({
                "feature_id": leaf.get("id"), "name": leaf.get("name"), "product": p,
                "note": f"样本内 {n} 个产品中,仅 {p} 在「{leaf.get('name')}」做到位",
            })
    return rows


def product_archetype(tree: dict, product: str) -> str:
    domains = tree.get("domains") or []
    covs = [(d, domain_coverage(d, product)) for d in domains]
    present = [(d, c) for d, c in covs if c["score"] is not None]
    if not present:
        return "数据不足"
    strong = [(d, c) for d, c in present if c["score"] >= _STRONG]
    breadth = len(present) / len(domains) if domains else 0.0
    strong_ratio = len(strong) / len(present)
    # 平台型:生态/平台域强 + 覆盖较广
    has_platform = any(d.get("role") in ("platform", "ecosystem") and c["score"] >= _STRONG
                       for d, c in present)
    if breadth >= _BREADTH_BROAD and strong_ratio >= _STRONG:
        return "平台型" if has_platform else "全能型"
    if strong and len(strong) <= 2 and strong_ratio <= 0.5:
        return "专精型"
    return "工具型"
```

- [ ] **Step 4: 跑测试确认 pass**

Run: `python -m pytest tests/test_feature_model.py::DiffMatrixTest tests/test_feature_model.py::ArchetypeTest -v`
Expected: PASS (4 passed)

- [ ] **Step 5: commit**

```bash
git add src/feature_model.py tests/test_feature_model.py
git commit -m "feat(feature): 样本内差异点矩阵 + product_archetype(确定性归纳)"
```

---

### Task 6: 护城河 + 蓝海公式

**Files:**
- Modify: `src/feature_model.py`
- Test: `tests/test_feature_model.py`

**Interfaces:**
- Consumes: `_leaves_of_domain` / `support_score` / `depth_norm`
- Produces:
  - `moat_candidates(tree: dict, product: str, migration_cost: Optional[str]=None) -> list[dict]` → 每项 `{"feature_id","name","domain","depth_score","factors","confidence","note"}`
  - `whitespace_opportunities(tree: dict, products: list[str]) -> list[dict]` → 每项 `{"feature_id","name","domain","reason","barrier"}`
- 模块常量：`_MOAT_WEIGHT_MIN = 0.2`，`_MOAT_DEPTH_MIN = 4`，`_WHITESPACE_WEIGHT_MIN = 0.2`，`_WHITESPACE_MAX_SUPPORT = 0.5`

- [ ] **Step 1: 写 failing 测试**

```python
# 追加到 tests/test_feature_model.py
from src.feature_model import moat_candidates, whitespace_opportunities


class MoatTest(unittest.TestCase):
    def _tree(self):
        return {"domains": [{"id": "A", "name": "可控性", "weight": 0.3, "modules": [
            {"id": "A1", "name": "运镜", "points": [
                {"id": "运镜控制", "name": "运镜控制", "moat_factors": ["专业用户沉淀"],
                 "products": {"Runway": _p("supported", 5, diff=True),
                              "Jimeng": _p("partial", 2)}},
            ]}]}]}

    def test_moat_needs_factor(self):
        out = moat_candidates(self._tree(), "Runway")
        self.assertEqual(len(out), 1)
        self.assertEqual(out[0]["confidence"], "high")
        self.assertIn("专业用户沉淀", out[0]["factors"])

    def test_strong_feature_without_factor_is_low_confidence(self):
        tree = self._tree()
        tree["domains"][0]["modules"][0]["points"][0]["moat_factors"] = []
        out = moat_candidates(tree, "Runway")  # 仍是候选,但需补难复制因素
        self.assertEqual(out[0]["confidence"], "low")
        self.assertIn("难复制", out[0]["note"])

    def test_migration_cost_adds_factor(self):
        tree = self._tree()
        tree["domains"][0]["modules"][0]["points"][0]["moat_factors"] = []
        out = moat_candidates(tree, "Runway", migration_cost="high")
        self.assertIn("用户习惯迁移成本", out[0]["factors"])

    def test_low_depth_is_not_moat(self):
        tree = self._tree()
        tree["domains"][0]["modules"][0]["points"][0]["products"]["Runway"]["depth_score"] = 2
        self.assertEqual(moat_candidates(tree, "Runway"), [])


class WhitespaceTest(unittest.TestCase):
    def test_high_weight_unmet_need_is_whitespace(self):
        tree = {"domains": [{"id": "A", "name": "可控性", "weight": 0.3, "modules": [
            {"id": "A1", "name": "多镜头", "points": [
                {"id": "多镜头小白化", "name": "多镜头小白化", "barrier": "现有方案学习成本高",
                 "products": {"Jimeng": _p("unsupported"), "Kling": _p("partial", 2)}},
            ]}]}]}
        out = whitespace_opportunities(tree, ["Jimeng", "Kling"])
        self.assertEqual(len(out), 1)
        self.assertIn("学习成本", out[0]["barrier"])

    def test_well_covered_need_is_not_whitespace(self):
        tree = {"domains": [{"id": "A", "name": "a", "weight": 0.3, "modules": [
            {"id": "A1", "name": "x", "points": [
                {"id": "x", "name": "x", "products": {"P": _p("supported", 4)}}]}]}]}
        self.assertEqual(whitespace_opportunities(tree, ["P"]), [])
```

- [ ] **Step 2: 跑测试确认 fail**

Run: `python -m pytest tests/test_feature_model.py::MoatTest tests/test_feature_model.py::WhitespaceTest -v`
Expected: FAIL（`cannot import name 'moat_candidates'`）

- [ ] **Step 3: 写实现（追加到 `src/feature_model.py`）**

```python
_MOAT_WEIGHT_MIN = 0.2
_MOAT_DEPTH_MIN = 4
_WHITESPACE_WEIGHT_MIN = 0.2
_WHITESPACE_MAX_SUPPORT = 0.5


def moat_candidates(tree: dict, product: str,
                    migration_cost: Optional[str] = None) -> list[dict]:
    """护城河候选 = 高权重能力 × 高深度分 × 样本内差异点 × 难复制因素。
    无难复制因素时仍列为候选,但 confidence=low + note 提示需补因素(防只凭功能有无推护城河)。"""
    out = []
    for domain in tree.get("domains") or []:
        if float(domain.get("weight", 0.0)) < _MOAT_WEIGHT_MIN:
            continue
        for leaf in _leaves_of_domain(domain):
            pdata = (leaf.get("products") or {}).get(product) or {}
            depth = pdata.get("depth_score")
            if depth is None or int(depth) < _MOAT_DEPTH_MIN:
                continue
            if not pdata.get("differentiator"):
                continue
            factors = list(leaf.get("moat_factors") or [])
            if migration_cost == "high" and "用户习惯迁移成本" not in factors:
                factors.append("用户习惯迁移成本")
            confidence = "high" if factors else "low"
            note = ("已叠加难复制因素" if factors
                    else "仅功能领先,缺难复制因素证据,需人工确认护城河成立")
            out.append({"feature_id": leaf.get("id"), "name": leaf.get("name"),
                        "domain": domain.get("name"), "depth_score": int(depth),
                        "factors": factors, "confidence": confidence, "note": note})
    return out


def whitespace_opportunities(tree: dict, products: list[str]) -> list[dict]:
    """蓝海 = 高权重需求 × 当前覆盖不足 × 门槛高。
    覆盖不足 = 样本内所有产品 support_score 均 ≤0.5(没人做到位),但至少有据可依(非全 unknown)。"""
    out = []
    for domain in tree.get("domains") or []:
        if float(domain.get("weight", 0.0)) < _WHITESPACE_WEIGHT_MIN:
            continue
        for leaf in _leaves_of_domain(domain):
            prods = leaf.get("products") or {}
            scores = [support_score((prods.get(p) or {}).get("support_status", "unknown"))
                      for p in products]
            known = [s for s in scores if s is not None]
            if not known:                          # 全 unknown:是未测量,不是蓝海
                continue
            if max(known) > _WHITESPACE_MAX_SUPPORT:
                continue
            barrier = leaf.get("barrier") or "现有方案门槛/成本/学习成本偏高(需人工确认)"
            out.append({"feature_id": leaf.get("id"), "name": leaf.get("name"),
                        "domain": domain.get("name"),
                        "reason": f"高权重域「{domain.get('name')}」下样本内无人做到位",
                        "barrier": barrier})
    return out
```

- [ ] **Step 4: 跑测试确认 pass**

Run: `python -m pytest tests/test_feature_model.py::MoatTest tests/test_feature_model.py::WhitespaceTest -v`
Expected: PASS (6 passed)

- [ ] **Step 5: commit**

```bash
git add src/feature_model.py tests/test_feature_model.py
git commit -m "feat(feature): 护城河(叠加难复制因素) + 蓝海(高价值未被低门槛解决)公式"
```

---

### Task 7: `compute_feature_analysis` 聚合器（引擎对外唯一入口）

**Files:**
- Modify: `src/feature_model.py`
- Test: `tests/test_feature_model.py`

**Interfaces:**
- Consumes: 本模块全部派生函数
- Produces:
  - `compute_feature_analysis(tree: dict, products: list[str], target: str, migration_cost: Optional[str]=None) -> dict`
  - 返回 schema：
    ```python
    {
      "feature_weight_version": str,
      "coverage": {<product>: {"coverage_known_only","evidence_coverage_rate","by_domain"}},
      "winners": [{"feature_id","name","winner","reason","confidence"}],   # 每叶子一条
      "differentiation_matrix": [...],
      "archetypes": {<product>: str},
      "moat_candidates": [...],          # 针对 target
      "whitespace": [...],
    }
    ```

- [ ] **Step 1: 写 failing 测试**

```python
# 追加到 tests/test_feature_model.py
from src.feature_model import compute_feature_analysis


class ComputeFeatureAnalysisTest(unittest.TestCase):
    def test_end_to_end_shape(self):
        out = compute_feature_analysis(TREE, ["Jimeng", "Kling"], target="Jimeng")
        self.assertEqual(out["feature_weight_version"], "demo-v1")
        self.assertIn("Jimeng", out["coverage"])
        self.assertIn("Kling", out["coverage"])
        self.assertTrue(all("winner" in w for w in out["winners"]))
        self.assertIn("Jimeng", out["archetypes"])
        # 同输入恒同输出
        out2 = compute_feature_analysis(TREE, ["Jimeng", "Kling"], target="Jimeng")
        self.assertEqual(out, out2)
```

- [ ] **Step 2: 跑测试确认 fail**

Run: `python -m pytest tests/test_feature_model.py::ComputeFeatureAnalysisTest -v`
Expected: FAIL（`cannot import name 'compute_feature_analysis'`）

- [ ] **Step 3: 写实现（追加到 `src/feature_model.py`）**

```python
def compute_feature_analysis(tree: dict, products: list[str], target: str,
                             migration_cost: Optional[str] = None) -> dict:
    """功能树派生的对外唯一入口(对偶 pricing_model.compute_product)。
    同输入恒同输出;缺数据处各派生函数已各自返回 None/空,不估算。"""
    winners = []
    for domain in tree.get("domains") or []:
        for leaf in _leaves_of_domain(domain):
            w = feature_winner(leaf, products)
            winners.append({"feature_id": leaf.get("id"), "name": leaf.get("name"), **w})
    return {
        "feature_weight_version": tree.get("feature_weight_version", "unversioned"),
        "coverage": {p: weighted_coverage(tree, p) for p in products},
        "winners": winners,
        "differentiation_matrix": differentiation_matrix(tree, products),
        "archetypes": {p: product_archetype(tree, p) for p in products},
        "moat_candidates": moat_candidates(tree, target, migration_cost),
        "whitespace": whitespace_opportunities(tree, products),
    }
```

- [ ] **Step 4: 跑测试确认 pass + 全引擎回归**

Run: `python -m pytest tests/test_feature_model.py tests/test_feature_weights.py -v`
Expected: PASS (全部通过)

- [ ] **Step 5: commit**

```bash
git add src/feature_model.py tests/test_feature_model.py
git commit -m "feat(feature): compute_feature_analysis 聚合器(引擎对外唯一入口)"
```

---

### 🚦 Phase 1 验收关卡（用户测试点）

引擎完全独立可验。请人工确认：

1. **跑全引擎测试**：`python -m pytest tests/test_feature_model.py tests/test_feature_weights.py -v` 全绿。
2. **手验一棵真实功能树**（用即梦/可灵/Runway 真数据手写一个 TREE dict，调 `compute_feature_analysis` 打印输出），核对：
   - 覆盖率两个数（功能覆盖率 vs 证据覆盖率）是否符合直觉；
   - winner 在「全员无 depth」时是否确实退成 tie/unclear；
   - 差异点措辞是否带「样本内 N 个产品中」、无「独占」；
   - 护城河候选在缺难复制因素时 confidence 是否为 low。
3. **确认权重表** `config/feature_weights.yaml` 两个 focus 的权重分配是否合理（这是 Part E 待确认项：初值现为手填，可后续接 LLM 提案）。

> 关卡通过后再进 Phase 2。Phase 2 起涉及 LLM 产出，结果不确定性高，务必逐阶段验。

---

# Phase 2 · analyzer 接引擎 + 产出新叶子 schema + §3 去 archetype

> 风险最高的一段：要改 LLM 产出的功能点字段。策略——把**确定性装配**（skeleton/归一化/引擎挂载）与 **LLM 填空**解耦，前者全部 TDD，后者留人工验收。

### Task 8: 功能树确定性装配工具 `_build_feature_skeleton` + `_normalize_leaf`

**Files:**
- Modify: `src/analyzer.py`（新增私有函数，靠近 `_feature_tree_call`，约 line 474 前）
- Test: `tests/test_feature_tree_assembly.py`

**Interfaces:**
- Consumes: `src.feature_weights.domains_for_focus` / `src.feature_weights.version`
- Produces（analyzer 模块内）：
  - `_build_feature_skeleton(meta: dict, flat_features: list[dict]) -> dict`：把现有扁平 `feature_spine`（`[{feature_id,name}]`）按 focus 的 domains 包成三层 tree（无法归类的功能进首个 domain 的「其它」module），挂 `feature_weight_version`。
  - `_normalize_leaf(legacy_pdata: dict) -> dict`：旧 `{support_status, quality_score{score}}` → 新 `{support_status(归一), depth_score, evidence_level, differentiator, source_refs}`。`quality_score.score>0` → `depth_score=score`；`==0`/缺 → `None`。`evidence_level` 由 `support_evidence_ids` 是否存在粗判（有→`official`，无→`inferred`）。

- [ ] **Step 1: 写 failing 测试**

```python
# tests/test_feature_tree_assembly.py
import unittest
from src.analyzer import _build_feature_skeleton, _normalize_leaf


class NormalizeLeafTest(unittest.TestCase):
    def test_quality_score_maps_to_depth(self):
        out = _normalize_leaf({"support_status": "partially_supported",
                               "support_evidence_ids": ["S1234567"],
                               "quality_score": {"score": 4, "scale": 5}})
        self.assertEqual(out["support_status"], "partial")
        self.assertEqual(out["depth_score"], 4)
        self.assertEqual(out["evidence_level"], "official")

    def test_zero_score_becomes_null_depth(self):
        out = _normalize_leaf({"support_status": "supported",
                               "support_evidence_ids": [],
                               "quality_score": {"score": 0, "scale": 5}})
        self.assertIsNone(out["depth_score"])
        self.assertEqual(out["evidence_level"], "inferred")
        self.assertFalse(out["differentiator"])


class SkeletonTest(unittest.TestCase):
    def test_wraps_flat_features_into_domains(self):
        meta = {"analysis_focus": ["视频生成质量与可控性"]}
        flat = [{"feature_id": "F001", "name": "文生视频"},
                {"feature_id": "F002", "name": "运镜控制"}]
        tree = _build_feature_skeleton(meta, flat)
        self.assertTrue(tree["domains"])
        self.assertEqual(tree["feature_weight_version"], "demo-v1")
        # 所有 flat feature 都落进了某个 domain 的某个 module 的 points
        ids = [pt["id"] for d in tree["domains"] for m in d["modules"] for pt in m["points"]]
        self.assertCountEqual(ids, ["F001", "F002"])
```

- [ ] **Step 2: 跑测试确认 fail**

Run: `python -m pytest tests/test_feature_tree_assembly.py -v`
Expected: FAIL（`cannot import name '_build_feature_skeleton'`）

- [ ] **Step 3: 写实现（在 `src/analyzer.py` 顶部 import 区加，并在 `_feature_tree_call` 之前加函数）**

import 区（靠近 line 74-76 的 pricing import）追加：
```python
from . import feature_weights
from .feature_model import normalize_status, compute_feature_analysis
```

函数实现：
```python
def _normalize_leaf(legacy_pdata: dict) -> dict:
    """旧叶子(support_status + quality_score.score) → 新叶子 schema(depth_score 等)。"""
    pdata = legacy_pdata or {}
    qs = pdata.get("quality_score") or {}
    score = qs.get("score")
    depth = int(score) if isinstance(score, (int, float)) and int(score) >= 1 else None
    refs = list(pdata.get("support_evidence_ids") or [])
    return {
        "support_status": normalize_status(pdata.get("support_status", "unknown")),
        "depth_score": depth,
        "evidence_level": "official" if refs else "inferred",
        "differentiator": bool(pdata.get("differentiator")),
        "source_refs": refs,
    }


def _build_feature_skeleton(meta: dict, flat_features: list[dict]) -> dict:
    """把扁平 feature_spine 包成 focus 加权三层 skeleton(叶子先空,后续填)。
    无法精确归类时,功能均匀落入各 domain 的『其它』module —— 装配确定、可复现。"""
    focus = (meta.get("analysis_focus") or [""])[0]
    domains_cfg = feature_weights.domains_for_focus(focus)
    domains = [{"id": d["id"], "name": d["name"], "weight": d["weight"],
                "role": d.get("role", "core"),
                "modules": [{"id": f"{d['id']}0", "name": "其它", "points": []}]}
               for d in domains_cfg]
    # 简单确定性分配:按出现顺序轮转进各 domain(真实归类后续由 LLM/规则细化)
    for i, f in enumerate(flat_features or []):
        target = domains[i % len(domains)] if domains else None
        if target is None:
            continue
        target["modules"][0]["points"].append(
            {"id": f.get("feature_id"), "name": f.get("name"), "products": {}})
    return {"feature_weight_version": feature_weights.version(), "domains": domains}
```

- [ ] **Step 4: 跑测试确认 pass**

Run: `python -m pytest tests/test_feature_tree_assembly.py -v`
Expected: PASS (3 passed)

- [ ] **Step 5: commit**

```bash
git add src/analyzer.py tests/test_feature_tree_assembly.py
git commit -m "feat(analyzer): 功能树确定性装配(skeleton 三层 + 旧叶子归一化)"
```

---

### Task 9: `_apply_feature_engine` —— 把派生分析挂上 facts

**Files:**
- Modify: `src/analyzer.py`（新增 `_apply_feature_engine`，并在 `analyzer_node` 调 `_apply_pricing_engine` 处旁边调用——见 line 922 附近）
- Test: `tests/test_feature_tree_assembly.py`

**Interfaces:**
- Consumes: `_build_feature_skeleton` / `_normalize_leaf` / `compute_feature_analysis`
- Produces:
  - `_apply_feature_engine(facts: dict, meta: dict) -> int`：读 `facts["feature_tree"]["features"]`（现有扁平结构），构建 skeleton + 把每个 feature 的 `products` 各产品叶子归一后填入 skeleton 对应 point，调 `compute_feature_analysis`，结果挂 `facts["feature_tree"]["tree"]`（三层）与 `facts["feature_tree"]["analysis"]`（派生）。返回处理的 feature 数。
  - 兼容：保留原 `facts["feature_tree"]["features"]` 不动（writer 旧路径不立刻断）。

- [ ] **Step 1: 写 failing 测试**

```python
# 追加到 tests/test_feature_tree_assembly.py
from src.analyzer import _apply_feature_engine


class ApplyFeatureEngineTest(unittest.TestCase):
    def test_attaches_tree_and_analysis(self):
        facts = {"feature_tree": {"features": [
            {"feature_id": "F001", "name": "文生视频", "products": {
                "Jimeng": {"support_status": "supported",
                           "support_evidence_ids": ["S1234567"],
                           "quality_score": {"score": 3, "scale": 5}},
                "Kling": {"support_status": "supported",
                          "support_evidence_ids": ["S7654321"],
                          "quality_score": {"score": 4, "scale": 5}}}},
        ]}}
        meta = {"analysis_focus": ["视频生成质量与可控性"],
                "target_product": "Jimeng", "competitors": ["Kling"]}
        n = _apply_feature_engine(facts, meta)
        self.assertEqual(n, 1)
        ft = facts["feature_tree"]
        self.assertIn("tree", ft)
        self.assertIn("analysis", ft)
        self.assertIn("Jimeng", ft["analysis"]["coverage"])
        # 叶子已是新 schema
        pt = ft["tree"]["domains"][0]["modules"][0]["points"][0]
        self.assertEqual(pt["products"]["Kling"]["depth_score"], 4)
```

- [ ] **Step 2: 跑测试确认 fail**

Run: `python -m pytest tests/test_feature_tree_assembly.py::ApplyFeatureEngineTest -v`
Expected: FAIL（`cannot import name '_apply_feature_engine'`）

- [ ] **Step 3: 写实现（在 `src/analyzer.py` 加函数）**

```python
def _apply_feature_engine(facts: dict, meta: dict) -> int:
    """把 Analyzer 抽到的功能事实接入确定性功能引擎(对偶 _apply_pricing_engine)。
    保留旧 features[] 不动;额外挂 feature_tree.tree(三层) + feature_tree.analysis(派生)。"""
    ft = facts.get("feature_tree") or {}
    flat = ft.get("features") or []
    if not flat:
        return 0
    products = [meta.get("target_product")] + list(meta.get("competitors") or [])
    products = [p for p in products if p]
    tree = _build_feature_skeleton(meta, flat)
    # 把扁平 features 的各产品叶子归一后填进 skeleton 同 id 的 point
    point_by_id = {pt["id"]: pt for d in tree["domains"] for m in d["modules"] for pt in m["points"]}
    for f in flat:
        pt = point_by_id.get(f.get("feature_id"))
        if pt is None:
            continue
        for product, pdata in (f.get("products") or {}).items():
            pt["products"][product] = _normalize_leaf(pdata)
    migration = ((facts.get("user_persona") or {}).get("migration_cost") or {}).get("level")
    analysis = compute_feature_analysis(
        tree, products, target=meta.get("target_product") or (products[0] if products else ""),
        migration_cost=migration)
    ft["tree"] = tree
    ft["analysis"] = analysis
    facts["feature_tree"] = ft
    return len(flat)
```

在 `analyzer_node`（line 922 附近，`enriched = _apply_pricing_engine(facts, evidence)` 之后）追加一行：
```python
        _apply_feature_engine(facts, meta)
```

- [ ] **Step 4: 跑测试确认 pass**

Run: `python -m pytest tests/test_feature_tree_assembly.py -v`
Expected: PASS (4 passed)

- [ ] **Step 5: commit**

```bash
git add src/analyzer.py tests/test_feature_tree_assembly.py
git commit -m "feat(analyzer): _apply_feature_engine 挂载三层树 + 派生分析"
```

---

### Task 10: 升级功能点 LLM fill 产出 depth_score/evidence_level/differentiator

**Files:**
- Modify: `src/analyzer.py`（`_feature_tree_call` 内 `_fill` 的 `fill_instruct` prompt，line 520-549）
- Test: 人工验收（LLM 产出不确定，无法 TDD；用 Task 9 的归一化兜底保证 schema 完整）

**Interfaces:**
- Consumes/Produces: 不改函数签名；扩 prompt 让 LLM 直接产出新字段，`_normalize_leaf` 仍兜底（双保险）。

- [ ] **Step 1: 在 `fill_instruct` 的 JSON 输出契约中增字段**（在 line 523-525 的 JSON 模板里，把 `quality_score` 段后增 `depth_score`/`evidence_level`/`differentiator` 说明）

把 JSON 契约改为（保留 quality_score 向后兼容，新增三字段）：
```python
            '只输出 JSON: {"products":{"F001":{"support_status":"supported|partial|'
            'unsupported|unknown","support_evidence_ids":["..."],'
            '"depth_score":1-5或null,"evidence_level":"official|third_party|user_review|inferred",'
            '"differentiator":true/false,'
            '"quality_score":{"score":0-5,"scale":5,"basis":"一句话依据","evidence_ids":["..."]}}}}。\n'
```

- [ ] **Step 2: 在 prompt 纪律段补 depth_score 评分口径**（在 line 538 `## 纪律` 段前插入）

```python
            "## depth_score 评分口径(强制,无证据写 null,绝不编分)\n"
            "1=仅入口/基础能力; 2=可用但限制明显; 3=主流可用效果稳定; "
            "4=质量/可控性明显优于多数竞品; 5=专业级,形成明显工作流优势。\n"
            "**仅当存在 官方说明/实测结果/第三方评测/用户案例 之一才给分,否则 depth_score=null。**\n"
            "evidence_level 取你给 depth_score 所依据证据的最高等级;differentiator=该能力是否"
            "在本次样本产品中明显仅此一家做到位(宁缺毋滥,不确定填 false)。\n"
```

- [ ] **Step 3: 人工验收（跑一次真实分析）**

Run（需 LLM key；DOMAIN 用即梦域）:
```bash
python -m src.graph --domain jimeng
```
检查产出 JSON（`data/debug/` 或最终 schema）中功能点是否带 `depth_score`（含 null）、`evidence_level`、`differentiator`，且 `_normalize_leaf` 对缺字段产出仍能兜底成完整 schema。

> 注：若 LLM 未产出新字段，`_normalize_leaf` 会从 `quality_score.score` 回填 depth_score——不阻断，但人工应核对覆盖度。

- [ ] **Step 4: commit**

```bash
git add src/analyzer.py
git commit -m "feat(analyzer): 功能点 fill 直接产出 depth_score/evidence_level/differentiator + 评分口径"
```

---

### Task 11: §3 定价 facts 去 archetype（移交 §10）

**Files:**
- Modify: `src/analyzer.py`（`_apply_pricing_engine` 后，标注 archetype 归属 §10；不在 facts 层删 engine 计算，仅约定 writer §3 不渲染 archetype——见 Phase 3 Task 14）
- Test: `tests/test_analyzer_pricing_engine.py`（已存在，加一条断言）

**Interfaces:**
- 说明：archetype 仍由 `compute_product` 算（数据需要），但**渲染归属**改变。本任务只加测试锁定「pricing facts 的对外语义不含 archetype 判断句」，真正的渲染迁移在 Task 14/16。此处仅确保 `pricing_engine.archetype` 字段存在供 §10 用。

- [ ] **Step 1: 加测试（锁定 archetype 字段供 §10 取用）**

```python
# 追加到 tests/test_analyzer_pricing_engine.py（若类不存在则新建）
# 断言 pricing_engine 仍产出 archetype(供 §10 商业模式逻辑渲染),且与 §3 facts 分离
def test_pricing_engine_exposes_archetype_for_section10(self):
    facts = {"pricing_model": {"products": [{
        "name": "即梦AI",
        "pricing_structure": {"layers": [
            {"mechanism": "freemium", "present": True},
            {"mechanism": "subscription", "present": True},
            {"mechanism": "credits", "present": True}]},
        "tiers": [{"tier_name": "标准", "billing_options": [
            {"cycle": "monthly", "is_promo": False, "price": {"amount": 199, "currency": "CNY"}}],
            "credit_grant": {"amount": 2210, "unit": "积分"}}],
        "consumption": []}]}}
    from src.analyzer import _apply_pricing_engine
    _apply_pricing_engine(facts, [])
    eng = facts["pricing_model"]["products"][0]["pricing_engine"]
    self.assertEqual(eng["archetype"], "Freemium + Subscription + Credits")
```

- [ ] **Step 2: 跑测试**

Run: `python -m pytest tests/test_analyzer_pricing_engine.py -v`
Expected: PASS（archetype 字段已由现有 `compute_product` 产出）

- [ ] **Step 3: commit**

```bash
git add tests/test_analyzer_pricing_engine.py
git commit -m "test(analyzer): 锁定 pricing_engine.archetype 供 §10 商业模式逻辑取用"
```

---

### 🚦 Phase 2 验收关卡（用户测试点）

1. **确定性测试全绿**：`python -m pytest tests/test_feature_tree_assembly.py tests/test_analyzer_pricing_engine.py -v`
2. **真实跑一次**（有 LLM key）：`python -m src.graph --domain jimeng`，导出 schema，人工核对：
   - `feature_tree.tree` 三层结构齐全、各 domain 权重来自 config；
   - `feature_tree.analysis` 覆盖率/winner/差异点/护城河/蓝海都有值且合理；
   - 功能点叶子含 `depth_score`（含合理的 null）、`evidence_level`、`differentiator`；
   - 无证据的 depth_score 确实是 null（抽查几条对照证据）。
3. **若覆盖率/winner 明显失真** → 多半是 LLM depth_score 乱给或 domain 归类轮转太粗（Task 8 用了轮转分配）。记录问题，决定是否需要 LLM 辅助归类（Part E）。

> 这是最容易出问题的一关。建议在此多跑几个 case。通过后进 Phase 3。

---

# Phase 3 · writer 物理重排为决策四层 + 新章节 + 定价 bug 修复

### Task 12: 决策摘要 §0 渲染器

**Files:**
- Modify: `src/writer.py`（新增 `_render_decision_summary`）
- Test: `tests/test_writer_decision_report.py`

**Interfaces:**
- Consumes: `schema["feature_tree"]["analysis"]`、`schema["pricing_model"]`、`schema["decision_summary"]`（若 analyzer 产出，见 Task 18）
- Produces: `_render_decision_summary(schema: dict, meta: dict) -> str`——固定回答 5 问，每条带 `confidence` 与 chip；缺数据写「证据不足」+ low。

- [ ] **Step 1: 写 failing 测试**

```python
# tests/test_writer_decision_report.py
import unittest
from src.writer import _render_decision_summary


class DecisionSummaryTest(unittest.TestCase):
    def test_renders_five_questions_with_confidence(self):
        schema = {"decision_summary": {
            "why_success": {"answer": "靠剪映生态导流", "confidence": "medium", "refs": ["S1234567"]},
            "how_monetize": {"answer": "低门槛积分扩规模", "confidence": "high", "refs": ["S7654321"]},
            "moat": {"answer": "生态型候选", "confidence": "medium", "refs": []},
            "what_to_learn": {"answer": "学可灵首尾帧", "confidence": "high", "refs": ["S1111111"]},
            "what_to_avoid": {"answer": "别拼专业运镜", "confidence": "medium", "refs": []},
        }}
        out = _render_decision_summary(schema, {})
        self.assertIn("决策摘要", out)
        for kw in ("为什么成功", "靠什么赚钱", "护城河", "该学什么", "该避开什么"):
            self.assertIn(kw, out)
        self.assertIn("置信度", out)
        self.assertIn("[S1234567]", out)

    def test_missing_summary_degrades_gracefully(self):
        out = _render_decision_summary({}, {})
        self.assertIn("证据不足", out)
```

- [ ] **Step 2: 跑测试确认 fail**

Run: `python -m pytest tests/test_writer_decision_report.py::DecisionSummaryTest -v`
Expected: FAIL（`cannot import name '_render_decision_summary'`）

- [ ] **Step 3: 写实现（追加到 `src/writer.py`）**

```python
_DECISION_QUESTIONS = [
    ("why_success", "为什么成功"),
    ("how_monetize", "靠什么赚钱"),
    ("moat", "护城河是什么"),
    ("what_to_learn", "我们该学什么"),
    ("what_to_avoid", "我们该避开什么"),
]
_CONF_CN = {"high": "高", "medium": "中", "low": "低"}


def _render_decision_summary(schema: dict, meta: dict) -> str:
    ds = schema.get("decision_summary") or {}
    lines = ["## 决策摘要", "", "> 只读一页:5 个终极问题,每条结论标注置信度。", ""]
    for key, label in _DECISION_QUESTIONS:
        item = ds.get(key) or {}
        answer = item.get("answer")
        if not answer:
            lines.append(f"- **{label}？** 证据不足（置信度低）")
            continue
        conf = _CONF_CN.get(item.get("confidence", "low"), "低")
        chips = "".join(f"[{r}]" for r in (item.get("refs") or []))
        lines.append(f"- **{label}？** {answer}（置信度{conf}）{chips}")
    return "\n".join(lines)
```

- [ ] **Step 4: 跑测试确认 pass**

Run: `python -m pytest tests/test_writer_decision_report.py::DecisionSummaryTest -v`
Expected: PASS (2 passed)

- [ ] **Step 5: commit**

```bash
git add src/writer.py tests/test_writer_decision_report.py
git commit -m "feat(writer): §0 决策摘要渲染器(五问 + confidence + chip)"
```

---

### Task 13: §5 技术能力渲染器 + §10 商业模式逻辑渲染器

**Files:**
- Modify: `src/writer.py`（新增 `_render_tech_capability`、`_render_business_model`）
- Test: `tests/test_writer_decision_report.py`

**Interfaces:**
- Produces:
  - `_render_tech_capability(schema: dict, products: list[str]) -> str`：读 `schema["tech_capability"]`（性能/质量/限制/模型版本等硬指标），无数据标 unknown；去重声明（功能树答能不能做，本节答到什么水平）。
  - `_render_business_model(pricing_model: dict) -> str`：承接 `pricing_engine.archetype` + `pricing_strategy_analysis`，输出判断句。

- [ ] **Step 1: 写 failing 测试**

```python
# 追加到 tests/test_writer_decision_report.py
from src.writer import _render_tech_capability, _render_business_model


class TechAndBizTest(unittest.TestCase):
    def test_tech_capability_marks_unknown(self):
        schema = {"tech_capability": {"products": {
            "Jimeng": {"max_resolution": "1080p", "max_duration": None,
                       "gen_speed": "约30s/条", "model_version": None,
                       "source_refs": ["S1234567"]}}}}
        out = _render_tech_capability(schema, ["Jimeng"])
        self.assertIn("技术能力", out)
        self.assertIn("1080p", out)
        self.assertIn("unknown", out.lower())  # max_duration/model_version 缺 → unknown

    def test_business_model_carries_archetype(self):
        pm = {"products": [{"name": "即梦AI", "pricing_engine": {
            "archetype": "Freemium + Subscription + Credits"}}],
            "pricing_strategy_analysis": {"pricing_model_analysis": {
                "summary": "定价应拆成三层看", "products": []}}}
        out = _render_business_model(pm)
        self.assertIn("商业模式逻辑", out)
        self.assertIn("Freemium + Subscription + Credits", out)
```

- [ ] **Step 2: 跑测试确认 fail**

Run: `python -m pytest tests/test_writer_decision_report.py::TechAndBizTest -v`
Expected: FAIL

- [ ] **Step 3: 写实现（追加到 `src/writer.py`）**

```python
_TECH_FIELDS = [("max_resolution", "最大分辨率"), ("max_duration", "最大时长"),
                ("gen_speed", "生成速度"), ("model_version", "模型版本/benchmark")]


def _render_tech_capability(schema: dict, products: list[str]) -> str:
    tc = (schema.get("tech_capability") or {}).get("products") or {}
    lines = ["## 技术能力",
             "", "> 功能树答『能不能做』,本节答『背后的性能/质量/限制到什么水平』。证据不足标 unknown。", "",
             "| 指标 | " + " | ".join(products) + " |",
             "|---|" + "|".join(["---"] * len(products)) + "|"]
    for field, label in _TECH_FIELDS:
        cells = []
        for p in products:
            v = (tc.get(p) or {}).get(field)
            cells.append(str(v) if v else "unknown")
        lines.append(f"| {label} | " + " | ".join(cells) + " |")
    return "\n".join(lines)


def _render_business_model(pricing_model: dict) -> str:
    lines = ["## 商业模式逻辑", "", "> 承接定价事实,落到『靠什么赚钱、为什么这么设计』的判断。", ""]
    for prod in pricing_model.get("products") or []:
        eng = prod.get("pricing_engine") or {}
        arch = eng.get("archetype")
        if arch:
            lines.append(f"- **{prod.get('name')}**：定价范式 `{arch}`")
    psa = (pricing_model.get("pricing_strategy_analysis") or {}).get("pricing_model_analysis") or {}
    if psa.get("summary"):
        lines += ["", psa["summary"]]
    for p in psa.get("products") or []:
        if p.get("business_logic"):
            lines.append(f"- **{p.get('product')}**：{p['business_logic']}")
    return "\n".join(lines)
```

- [ ] **Step 4: 跑测试确认 pass**

Run: `python -m pytest tests/test_writer_decision_report.py::TechAndBizTest -v`
Expected: PASS (2 passed)

- [ ] **Step 5: commit**

```bash
git add src/writer.py tests/test_writer_decision_report.py
git commit -m "feat(writer): §5 技术能力 + §10 商业模式逻辑渲染器"
```

---

### Task 14: 定价 §3 去 archetype + 顶表 bug 修复

**Files:**
- Modify: `src/writer.py`（`_render_pricing`，line 596-735）
- Test: `tests/test_pricing_extraction.py`（已存在）或 `tests/test_writer_decision_report.py`

**Interfaces:**
- 改 `_render_pricing`：(1) 顶部价格表当 tier 仅有 `billing_options`（无旧 `tier.price`）时，fallback 用 `pricing_model.regular_monthly_price(tier)` 渲染常规月价，不再显示 `—`；(2) 移除本函数内 archetype/商业模式判断句（迁至 §10）。

- [ ] **Step 1: 写 failing 测试（顶表 fallback）**

```python
# 追加到 tests/test_writer_decision_report.py
from src.writer import _render_pricing


class PricingTopTableTest(unittest.TestCase):
    def test_new_schema_tier_shows_regular_monthly_not_dash(self):
        pm = {"products": [{"name": "即梦AI", "tiers": [
            {"tier_name": "标准会员", "billing_options": [
                {"cycle": "monthly", "is_promo": False, "price": {"amount": 199, "currency": "CNY"}}]}]}]}
        out = _render_pricing(pm, {}, ["即梦AI"])
        self.assertIn("199", out)        # 顶表读出常规月价
        # archetype 判断句不应出现在 §3
        self.assertNotIn("archetype", out.lower())
```

- [ ] **Step 2: 跑测试确认 fail**

Run: `python -m pytest tests/test_writer_decision_report.py::PricingTopTableTest -v`
Expected: FAIL（顶表显示 `—`，无 199）

- [ ] **Step 3: 改实现**

在 `src/writer.py` 顶部 import 区加：
```python
from .pricing_model import regular_monthly_price
```
在 `_render_pricing` 内顶表读价处（当前读 `tier.get("price")`），改为 fallback：
```python
        price = tier.get("price")
        if price in (None, "", "—") and tier.get("billing_options"):
            rm = regular_monthly_price(tier)
            price = f"{rm:g}" if rm is not None else "—"
```
并删除/迁出本函数中任何 archetype/商业模式判断句到 §10（若有 `_render_pricing` 内拼 archetype 的行，移除）。

- [ ] **Step 4: 跑测试确认 pass**

Run: `python -m pytest tests/test_writer_decision_report.py::PricingTopTableTest tests/test_pricing_extraction.py -v`
Expected: PASS

- [ ] **Step 5: commit**

```bash
git add src/writer.py tests/test_writer_decision_report.py
git commit -m "fix(writer): §3 顶表新 schema fallback 读常规月价 + 去 archetype 判断句"
```

---

### Task 15: §7 功能覆盖与差距渲染（消费 analysis 派生）

**Files:**
- Modify: `src/writer.py`（`_render_feature_gaps`，line 520-595；或新增 `_render_feature_coverage` 并替换）
- Test: `tests/test_writer_decision_report.py`

**Interfaces:**
- Consumes: `schema["feature_tree"]["analysis"]`（coverage/winners/differentiation_matrix）
- Produces: `_render_feature_coverage(feature_tree: dict, products: list[str]) -> str`——输出每产品「功能覆盖率 X% / 证据覆盖率 Y%」、winner 列表（带 confidence，tie/unclear 如实写）、样本内差异点矩阵。

- [ ] **Step 1: 写 failing 测试**

```python
# 追加到 tests/test_writer_decision_report.py
from src.writer import _render_feature_coverage


class FeatureCoverageTest(unittest.TestCase):
    def test_renders_two_coverage_numbers_and_winner(self):
        ft = {"analysis": {
            "coverage": {"Jimeng": {"coverage_known_only": 0.62, "evidence_coverage_rate": 0.76, "by_domain": []}},
            "winners": [{"feature_id": "F1", "name": "运镜控制", "winner": "Runway",
                         "reason": "领先", "confidence": "high"},
                        {"feature_id": "F2", "name": "文生视频", "winner": "tie",
                         "reason": "无深度", "confidence": "low"}],
            "differentiation_matrix": [{"feature_id": "F3", "name": "多镜头",
                                        "product": "Runway", "note": "样本内 3 个产品中,仅 Runway 做到位"}]}}
        out = _render_feature_coverage(ft, ["Jimeng"])
        self.assertIn("功能覆盖率", out)
        self.assertIn("62%", out)
        self.assertIn("证据覆盖率", out)
        self.assertIn("76%", out)
        self.assertIn("Runway", out)
        self.assertIn("样本内 3 个产品中", out)
        self.assertNotIn("独占", out)
```

- [ ] **Step 2: 跑测试确认 fail**

Run: `python -m pytest tests/test_writer_decision_report.py::FeatureCoverageTest -v`
Expected: FAIL

- [ ] **Step 3: 写实现（追加到 `src/writer.py`）**

```python
def _render_feature_coverage(feature_tree: dict, products: list[str]) -> str:
    analysis = feature_tree.get("analysis") or {}
    cov = analysis.get("coverage") or {}
    lines = ["## 功能覆盖与差距", ""]
    for p in products:
        c = cov.get(p) or {}
        ck = c.get("coverage_known_only")
        ev = c.get("evidence_coverage_rate")
        ck_s = f"{ck*100:.0f}%" if ck is not None else "?"
        ev_s = f"{ev*100:.0f}%" if ev is not None else "?"
        lines.append(f"- **{p}**：功能覆盖率 {ck_s}，证据覆盖率 {ev_s}")
    lines += ["", "### 单项胜负（缺深度证据时如实判 tie/unclear）", ""]
    for w in analysis.get("winners") or []:
        conf = _CONF_CN.get(w.get("confidence", "low"), "低")
        lines.append(f"- {w.get('name')}：**{w.get('winner')}**（置信度{conf}）— {w.get('reason')}")
    dm = analysis.get("differentiation_matrix") or []
    if dm:
        lines += ["", "### 样本内差异点", ""]
        for r in dm:
            lines.append(f"- {r.get('note')}")
    return "\n".join(lines)
```

- [ ] **Step 4: 跑测试确认 pass**

Run: `python -m pytest tests/test_writer_decision_report.py::FeatureCoverageTest -v`
Expected: PASS

- [ ] **Step 5: commit**

```bash
git add src/writer.py tests/test_writer_decision_report.py
git commit -m "feat(writer): §7 功能覆盖与差距(双覆盖率 + winner + 样本内差异点)"
```

---

### Task 16: §9 功能定位+护城河+蓝海 + §13 口径锁定表

**Files:**
- Modify: `src/writer.py`（新增 `_render_feature_insights`、`_render_caliber_lock`）
- Test: `tests/test_writer_decision_report.py`

**Interfaces:**
- Produces:
  - `_render_feature_insights(feature_tree: dict, target: str) -> str`：archetypes + moat_candidates + whitespace。
  - `_render_caliber_lock(schema: dict, meta: dict) -> str`：口径锁定表（analysis_focus / selected_competitors / comparison_scope / pricing_currency / pricing_period / unit_cost_formula / feature_weight_version / unknown_handling_rule / generated_at）。

- [ ] **Step 1: 写 failing 测试**

```python
# 追加到 tests/test_writer_decision_report.py
from src.writer import _render_feature_insights, _render_caliber_lock


class InsightsAndLockTest(unittest.TestCase):
    def test_insights_render(self):
        ft = {"analysis": {
            "archetypes": {"Jimeng": "工具型", "Runway": "专精型"},
            "moat_candidates": [{"name": "运镜控制", "domain": "可控性", "depth_score": 5,
                                 "factors": ["专业用户沉淀"], "confidence": "high", "note": "已叠加难复制因素"}],
            "whitespace": [{"name": "多镜头小白化", "domain": "可控性",
                            "reason": "样本内无人做到位", "barrier": "学习成本高"}]}}
        out = _render_feature_insights(ft, "Jimeng")
        self.assertIn("护城河", out)
        self.assertIn("蓝海", out)
        self.assertIn("运镜控制", out)
        self.assertIn("多镜头小白化", out)

    def test_caliber_lock_has_all_fields(self):
        schema = {"feature_tree": {"analysis": {"feature_weight_version": "demo-v1"}}}
        meta = {"analysis_focus": ["视频质量"], "competitors": ["Kling"], "target_product": "Jimeng",
                "generated_at": "2026-06-17T00:00:00Z"}
        out = _render_caliber_lock(schema, meta)
        for kw in ("口径锁定", "demo-v1", "unknown", "元/积分", "Kling"):
            self.assertIn(kw, out)
```

- [ ] **Step 2: 跑测试确认 fail**

Run: `python -m pytest tests/test_writer_decision_report.py::InsightsAndLockTest -v`
Expected: FAIL

- [ ] **Step 3: 写实现（追加到 `src/writer.py`）**

```python
def _render_feature_insights(feature_tree: dict, target: str) -> str:
    a = feature_tree.get("analysis") or {}
    lines = ["## 功能定位 + 护城河 + 蓝海", "", "### 产品形态", ""]
    for p, arch in (a.get("archetypes") or {}).items():
        lines.append(f"- {p}：{arch}")
    lines += ["", "### 护城河候选（公式：高权重 × 高深度 × 样本内差异点 × 难复制因素）", ""]
    for m in a.get("moat_candidates") or []:
        conf = _CONF_CN.get(m.get("confidence", "low"), "低")
        factors = "、".join(m.get("factors") or []) or "（缺难复制因素，需人工确认）"
        lines.append(f"- {m.get('name')}（{m.get('domain')}，深度{m.get('depth_score')}，置信度{conf}）：{factors}")
    lines += ["", "### 蓝海机会（公式：高权重需求 × 覆盖不足 × 门槛高）", ""]
    for w in a.get("whitespace") or []:
        lines.append(f"- {w.get('name')}（{w.get('domain')}）：{w.get('reason')}；门槛——{w.get('barrier')}")
    return "\n".join(lines)


def _render_caliber_lock(schema: dict, meta: dict) -> str:
    fwv = ((schema.get("feature_tree") or {}).get("analysis") or {}).get("feature_weight_version", "unversioned")
    rows = [
        ("analysis_focus", " / ".join(meta.get("analysis_focus") or []) or "—"),
        ("selected_competitors", " / ".join(meta.get("competitors") or []) or "—"),
        ("comparison_scope", f"target={meta.get('target_product','—')}"),
        ("pricing_currency", "CNY（混币时如实标注，不跨币比较）"),
        ("pricing_period", "归一为月（年付÷12，季付÷3）"),
        ("unit_cost_formula", "元/积分 = 续费常规月价 ÷ 月积分；单位成本 = 元/积分 × 单位耗分"),
        ("feature_weight_version", fwv),
        ("unknown_handling_rule", "unknown 不进覆盖率分子/分母，单列证据覆盖率"),
        ("generated_at", meta.get("generated_at", "—")),
    ]
    lines = ["## 口径锁定表", "", "| 口径项 | 取值 |", "|---|---|"]
    lines += [f"| {k} | {v} |" for k, v in rows]
    return "\n".join(lines)
```

- [ ] **Step 4: 跑测试确认 pass**

Run: `python -m pytest tests/test_writer_decision_report.py::InsightsAndLockTest -v`
Expected: PASS

- [ ] **Step 5: commit**

```bash
git add src/writer.py tests/test_writer_decision_report.py
git commit -m "feat(writer): §9 功能定位/护城河/蓝海 + §13 口径锁定表"
```

---

### Task 17: writer_node 四层物理重排

**Files:**
- Modify: `src/writer.py`（`writer_node`，line 1018-1050）
- Test: `tests/test_writer_decision_report.py`

**Interfaces:**
- Consumes: 上述全部新渲染器 + 既有渲染器
- Produces: `writer_node` 按 B.0 四层骨架顺序组织 sections。

- [ ] **Step 1: 写 failing 测试（四层顺序 golden）**

```python
# 追加到 tests/test_writer_decision_report.py
from src.writer import writer_node


class FourLayerOrderTest(unittest.TestCase):
    def test_section_order_matches_decision_layers(self):
        state = {
            "schema_draft": {
                "decision_summary": {}, "competitor_landscape": {}, "positioning_map": {},
                "feature_tree": {"features": [], "analysis": {"coverage": {}, "winners": [],
                    "differentiation_matrix": [], "archetypes": {}, "moat_candidates": [],
                    "whitespace": [], "feature_weight_version": "demo-v1"}},
                "pricing_model": {"products": []}, "user_persona": {},
                "tech_capability": {"products": {}}, "swot": {}, "recommendations": [],
            },
            "analysis_meta": {"target_product": "Jimeng", "competitors": ["Kling"],
                              "analysis_focus": ["视频质量"], "generated_at": "2026-06-17"},
            "raw_evidence": [], "collection_meta": {},
        }
        out = writer_node(state)["report_draft"]
        # 决策摘要在最前;事实层在比较层前;口径锁定表在最后区
        i_summary = out.find("决策摘要")
        i_facts = out.find("功能树")
        i_coverage = out.find("功能覆盖与差距")
        i_insights = out.find("护城河")
        i_lock = out.find("口径锁定")
        self.assertTrue(0 <= i_summary < i_facts < i_coverage < i_insights < i_lock)
```

- [ ] **Step 2: 跑测试确认 fail**

Run: `python -m pytest tests/test_writer_decision_report.py::FourLayerOrderTest -v`
Expected: FAIL

- [ ] **Step 3: 改 `writer_node` 的 sections 列表为四层顺序**

```python
    feature_tree = schema.get("feature_tree") or {}
    pricing_model = schema.get("pricing_model") or {}
    target = meta.get("target_product", "")
    sections = [
        _render_header(meta, target=target,
                       competitors=list(meta.get("competitors") or []),
                       focus=list(meta.get("analysis_focus") or [])),
        _render_decision_summary(schema, meta),
        # ── 事实层 FACTS ──
        _render_competitor_landscape(schema.get("competitor_landscape") or {}),
        _render_score_overview(feature_tree, products, evidence),   # §2 功能树·事实
        _render_pricing(pricing_model, feature_tree, products),     # §3 定价·事实
        _render_personas(schema.get("user_persona") or {}, evidence),  # §4 用户画像
        _render_tech_capability(schema, products),                  # §5 技术能力
        # ── 比较层 COMPARISON ──
        _render_positioning_map(schema.get("positioning_map") or {}),  # §6
        _render_feature_coverage(feature_tree, products),           # §7
        # §8 定价同口径对比由 _render_pricing 内的对比段或单独渲染器承担(沿用现状)
        # ── 洞察层 INSIGHTS ──
        _render_feature_insights(feature_tree, target),             # §9
        _render_business_model(pricing_model),                      # §10
        _render_swot(schema.get("swot") or {}),                     # §11
        # ── 决策层 DECISIONS ──
        _render_recommendations(schema.get("recommendations") or [], schema),  # §12
        # ── 支撑 APPENDIX ──
        _render_data_availability(quality_audit),
        _render_evidence_coverage(evidence),
        _render_uncertainty(evidence, schema),
        _render_caliber_lock(schema, meta),                         # §13 口径锁定表
    ]
```

- [ ] **Step 4: 跑测试确认 pass + writer 既有回归**

Run: `python -m pytest tests/test_writer_decision_report.py tests/test_writer_rules.py -v`
Expected: PASS（如 `test_writer_rules` 因章节顺序断言失败，按四层新顺序更新其断言）

- [ ] **Step 5: commit**

```bash
git add src/writer.py tests/test_writer_decision_report.py tests/test_writer_rules.py
git commit -m "feat(writer): writer_node 物理重排为决策四层 + §0/§5/§9/§10/§13"
```

---

### 🚦 Phase 3 验收关卡（用户测试点）

1. **writer 测试全绿**：`python -m pytest tests/test_writer_decision_report.py tests/test_writer_rules.py tests/test_pricing_extraction.py -v`
2. **跑一次端到端导出 Markdown**，人工通读：
   - 章节顺序是否真的「决策摘要 → 事实 → 比较 → 洞察 → 决策 → 支撑」；
   - §3 定价不再出现 `—` 顶表（新 schema 产品有常规月价）、不再有 archetype 判断句；
   - §10 商业模式逻辑确实接住了 archetype；
   - §13 口径锁定表九项齐全。
3. **每条结论是否仍带 chip `[SXXXXXXX]`**（R9 自检不塌）。

---

# Phase 4 · reviewer 抗假精确校验

### Task 18: 三条新校验 + 注册

**Files:**
- Modify: `src/reviewer.py`（新增 3 个 `check_*`，注册进 `RULE_RUNNERS` + `MODE_CONFIG` + `ISSUE_TYPE_TO_TARGET`）
- Test: `tests/test_reviewer_feature_checks.py`

**Interfaces:**
- Produces（签名同其它 rule runner：`(schema, evidence) -> list[dict]`）：
  - `check_feature_depth_honesty`：叶子有 `depth_score` 非 null 但 `source_refs` 为空 → R11 error（编分）。
  - `check_differentiator_wording`：`differentiation_matrix` note 含「独占」→ R12 warning。
  - `check_winner_conservatism`：winner 为具体产品但该 leaf 全员无 depth → R13 error（违反保守口径）。
- 注册：`RULE_RUNNERS` 增 `"R11"/"R12"/"R13"`；`MODE_CONFIG.minimal.soft` 加 R12，`hard_gate` 加 R11/R13（full 同入 hard_gate）；`ISSUE_TYPE_TO_TARGET` 增 `feature_depth_fabricated→analyzer`、`differentiator_absolutism→writer`、`winner_overclaim→analyzer`。

- [ ] **Step 1: 写 failing 测试**

```python
# tests/test_reviewer_feature_checks.py
import unittest
from src.reviewer import (
    check_feature_depth_honesty, check_differentiator_wording, check_winner_conservatism,
)


def _tree_leaf(depth, refs):
    return {"feature_tree": {"tree": {"domains": [{"id": "A", "name": "a", "weight": 1.0,
        "modules": [{"id": "A1", "name": "a", "points": [
            {"id": "F1", "name": "x", "products": {
                "P": {"support_status": "supported", "depth_score": depth,
                      "evidence_level": "official", "differentiator": False, "source_refs": refs}}}]}]}]}}}


class DepthHonestyTest(unittest.TestCase):
    def test_depth_without_refs_is_error(self):
        issues = check_feature_depth_honesty(_tree_leaf(4, []), [])
        self.assertTrue(any(i["rule"] == "R11" for i in issues))

    def test_depth_with_refs_ok(self):
        self.assertEqual(check_feature_depth_honesty(_tree_leaf(4, ["S1234567"]), []), [])

    def test_null_depth_ok(self):
        self.assertEqual(check_feature_depth_honesty(_tree_leaf(None, []), []), [])


class WordingTest(unittest.TestCase):
    def test_dute_wording_flagged(self):
        schema = {"feature_tree": {"analysis": {"differentiation_matrix": [
            {"name": "x", "product": "P", "note": "P 独占该能力"}]}}}
        issues = check_differentiator_wording(schema, [])
        self.assertTrue(any(i["rule"] == "R12" for i in issues))


class WinnerConservatismTest(unittest.TestCase):
    def test_winner_without_any_depth_is_error(self):
        schema = {"feature_tree": {
            "tree": {"domains": [{"id": "A", "name": "a", "weight": 1.0, "modules": [
                {"id": "A1", "name": "a", "points": [{"id": "F1", "name": "x", "products": {
                    "P": {"support_status": "supported", "depth_score": None},
                    "Q": {"support_status": "supported", "depth_score": None}}}]}]}]},
            "analysis": {"winners": [{"feature_id": "F1", "name": "x", "winner": "P",
                                      "confidence": "high"}]}}}
        issues = check_winner_conservatism(schema, [])
        self.assertTrue(any(i["rule"] == "R13" for i in issues))
```

- [ ] **Step 2: 跑测试确认 fail**

Run: `python -m pytest tests/test_reviewer_feature_checks.py -v`
Expected: FAIL（`cannot import name 'check_feature_depth_honesty'`）

- [ ] **Step 3: 写实现（追加到 `src/reviewer.py`，紧跟现有 `check_*` 后、`RULE_RUNNERS` 前）**

```python
def _iter_tree_leaves(schema: dict):
    tree = (schema.get("feature_tree") or {}).get("tree") or {}
    for d in tree.get("domains") or []:
        for m in d.get("modules") or []:
            for pt in m.get("points") or []:
                yield pt


def check_feature_depth_honesty(schema: dict, evidence: list[dict]) -> list[dict]:
    """R11:depth_score 非 null 但无 source_refs = 编分,error。"""
    issues = []
    for pt in _iter_tree_leaves(schema):
        for prod, pdata in (pt.get("products") or {}).items():
            d = pdata.get("depth_score")
            if d is not None and not (pdata.get("source_refs") or []):
                issues.append(_mk_issue(
                    "R11", "feature_depth_fabricated",
                    f"feature_tree.tree.{pt.get('id')}.{prod}.depth_score",
                    f"{prod} 在「{pt.get('name')}」给了 depth_score={d} 但 source_refs 为空(疑似编分)"))
    return issues


def check_differentiator_wording(schema: dict, evidence: list[dict]) -> list[dict]:
    """R12:样本内差异点措辞出现「独占」= 绝对化,warning。"""
    issues = []
    dm = ((schema.get("feature_tree") or {}).get("analysis") or {}).get("differentiation_matrix") or []
    for r in dm:
        if "独占" in (r.get("note") or ""):
            issues.append(_mk_issue(
                "R12", "differentiator_absolutism",
                f"feature_tree.analysis.differentiation_matrix.{r.get('name')}",
                "差异点措辞含『独占』,应改为『样本内 N 个产品中』"))
    return issues


def check_winner_conservatism(schema: dict, evidence: list[dict]) -> list[dict]:
    """R13:winner 判了具体产品,但该 leaf 全员无 depth_score = 违反保守口径,error。"""
    issues = []
    leaves = {pt.get("id"): pt for pt in _iter_tree_leaves(schema)}
    winners = ((schema.get("feature_tree") or {}).get("analysis") or {}).get("winners") or []
    for w in winners:
        winner = w.get("winner")
        if winner in ("tie", "unclear", None):
            continue
        pt = leaves.get(w.get("feature_id"))
        if not pt:
            continue
        any_depth = any((pd or {}).get("depth_score") is not None
                        for pd in (pt.get("products") or {}).values())
        if not any_depth:
            issues.append(_mk_issue(
                "R13", "winner_overclaim",
                f"feature_tree.analysis.winners.{w.get('feature_id')}",
                f"「{w.get('name')}」全员无 depth_score 却判 winner={winner},应为 tie/unclear"))
    return issues
```

注册（改 `RULE_RUNNERS` dict，line 362 区）：
```python
    "R11": check_feature_depth_honesty,
    "R12": check_differentiator_wording,
    "R13": check_winner_conservatism,
```

改 `MODE_CONFIG`（line 34）：
```python
    "minimal": {
        "hard_gate": {"R1", "R4", "R5", "R9", "R11", "R13"},
        "soft": {"R0", "R2", "R3", "R7", "R10", "R12"},
        "llm": False,
    },
    "full": {
        "hard_gate": {"R0", "R1", "R2", "R3", "R4", "R5", "R9", "R10", "R11", "R13"},
        "soft": {"R7", "R12"},
        "llm": True,
    },
```

改 `ISSUE_TYPE_TO_TARGET`（line 47）增：
```python
    "feature_depth_fabricated": "analyzer",
    "differentiator_absolutism": "writer",
    "winner_overclaim": "analyzer",
```

- [ ] **Step 4: 跑测试确认 pass + reviewer 既有回归**

Run: `python -m pytest tests/test_reviewer_feature_checks.py -v`
Expected: PASS (6 passed)

- [ ] **Step 5: commit**

```bash
git add src/reviewer.py tests/test_reviewer_feature_checks.py
git commit -m "feat(reviewer): R11 深度诚实/R12 差异点措辞/R13 winner 保守口径"
```

---

# Phase 5 · 决策摘要 + Learn/Avoid/Attack 建议（analyzer 产出 + writer + reviewer）

### Task 19: recommendations 升级为三分块 schema + writer §12

**Files:**
- Modify: `src/analyzer.py`（derivations 阶段 recommendations prompt，line 280-300 区）、`src/writer.py`（`_render_recommendations`，line 925）
- Test: `tests/test_writer_decision_report.py`

**Interfaces:**
- 新建议 schema（追加字段，向后兼容旧）：`{action_type: learn|avoid|attack, action, target_competitor, evidence_refs, priority_score(0..100), rationale, risk}`。
- `_render_recommendations` 按 `action_type` 分 12.1 Learn / 12.2 Avoid / 12.3 Attack 三子块；缺 action_type 的旧建议归入「其它」。

- [ ] **Step 1: 写 failing 测试**

```python
# 追加到 tests/test_writer_decision_report.py
from src.writer import _render_recommendations


class RecommendationsTriBlockTest(unittest.TestCase):
    def test_groups_into_learn_avoid_attack(self):
        recs = [
            {"action_type": "learn", "action": "学首尾帧", "target_competitor": "Kling",
             "evidence_refs": ["S1234567"], "priority_score": 80, "rationale": "高权重", "risk": "投入大"},
            {"action_type": "attack", "action": "做多镜头小白化", "target_competitor": "Runway",
             "evidence_refs": [], "priority_score": 65, "rationale": "蓝海", "risk": "教育成本"},
        ]
        out = _render_recommendations(recs, {})
        self.assertIn("Learn", out)
        self.assertIn("Avoid", out)
        self.assertIn("Attack", out)
        self.assertIn("学首尾帧", out)
        self.assertIn("[S1234567]", out)
        self.assertIn("风险", out)
```

- [ ] **Step 2: 跑测试确认 fail**

Run: `python -m pytest tests/test_writer_decision_report.py::RecommendationsTriBlockTest -v`
Expected: FAIL

- [ ] **Step 3: 改 `_render_recommendations`**

```python
_ACTION_BLOCKS = [("learn", "12.1 Learn — 学竞品已验证强项"),
                  ("avoid", "12.2 Avoid — 避开竞品高权重领先区"),
                  ("attack", "12.3 Attack — 切入高价值空白区")]


def _render_recommendations(recs: list[dict], schema=None) -> str:
    lines = ["## 优先级建议", ""]
    by_type = {k: [] for k, _ in _ACTION_BLOCKS}
    other = []
    for r in recs or []:
        by_type.get(r.get("action_type"), other).append(r) if r.get("action_type") in by_type else other.append(r)
    for key, title in _ACTION_BLOCKS:
        items = sorted(by_type[key], key=lambda x: x.get("priority_score", 0), reverse=True)
        if not items:
            continue
        lines += [f"### {title}", ""]
        for r in items:
            chips = "".join(f"[{e}]" for e in (r.get("evidence_refs") or r.get("evidence_ids") or []))
            lines.append(
                f"- **{r.get('action','')}**（优先级 {r.get('priority_score','?')}，"
                f"对标 {r.get('target_competitor','—')}）{chips}")
            if r.get("rationale"):
                lines.append(f"  - 依据：{r['rationale']}")
            if r.get("risk"):
                lines.append(f"  - 风险：{r['risk']}")
        lines.append("")
    if other:
        lines += ["### 其它建议", ""]
        for r in other:
            lines.append(f"- {r.get('action','')}")
    return "\n".join(lines)
```

- [ ] **Step 4: 改 analyzer derivations recommendations prompt**（line 280-300 区）：在输出契约里要求每条建议带 `action_type/target_competitor/evidence_refs/priority_score/risk`，并提示 Learn 取 winner 强项、Avoid 取对手高权重领先、Attack 取 whitespace。

在 recommendations 的 JSON 契约说明处追加：
```python
        "每条建议必须含: action_type(learn|avoid|attack), action, target_competitor, "
        "evidence_refs(只用真实 evidence_id), priority_score(0-100), rationale, risk。\n"
        "Learn=学对手 winner 强项; Avoid=避开对手高权重且领先的能力域; "
        "Attack=切入 whitespace 蓝海。证据不足的建议不要编,宁可少出。\n"
```

- [ ] **Step 5: 跑测试确认 pass**

Run: `python -m pytest tests/test_writer_decision_report.py::RecommendationsTriBlockTest -v`
Expected: PASS

- [ ] **Step 6: commit**

```bash
git add src/analyzer.py src/writer.py tests/test_writer_decision_report.py
git commit -m "feat(rec): 建议升级 Learn/Avoid/Attack 三分块 + target/risk/priority schema"
```

---

### Task 20: analyzer 产出 decision_summary（§0 数据）

**Files:**
- Modify: `src/analyzer.py`（derivations 阶段新增 decision_summary 产出，或确定性从 analysis 派生兜底）
- Test: `tests/test_writer_decision_report.py`（已有 §0 渲染测试，本任务确保 schema 有数据来源）

**Interfaces:**
- Produces: `facts/derivations` 产出 `schema["decision_summary"]`，五问各 `{answer, confidence, refs}`。
- 兜底：新增确定性 `_fallback_decision_summary(schema) -> dict`——从 `feature_tree.analysis`（archetype/moat/winner）+ `pricing_engine.archetype` 拼出保守答案与 confidence（缺数据→low + 「证据不足」），LLM 产出缺失时用它。

- [ ] **Step 1: 写 failing 测试**

```python
# 追加到 tests/test_writer_decision_report.py
from src.analyzer import _fallback_decision_summary


class FallbackDecisionSummaryTest(unittest.TestCase):
    def test_derives_from_analysis(self):
        schema = {"feature_tree": {"analysis": {
            "archetypes": {"Jimeng": "工具型"},
            "moat_candidates": [{"name": "风格控制", "confidence": "medium", "factors": ["素材生态"]}]}},
            "pricing_model": {"products": [{"name": "Jimeng",
                "pricing_engine": {"archetype": "Freemium + Subscription + Credits"}}]}}
        ds = _fallback_decision_summary(schema, target="Jimeng")
        self.assertIn("moat", ds)
        self.assertIn("how_monetize", ds)
        self.assertIn(ds["moat"]["confidence"], ("high", "medium", "low"))
```

- [ ] **Step 2: 跑测试确认 fail**

Run: `python -m pytest tests/test_writer_decision_report.py::FallbackDecisionSummaryTest -v`
Expected: FAIL

- [ ] **Step 3: 写实现（追加到 `src/analyzer.py`）**

```python
def _fallback_decision_summary(schema: dict, target: str) -> dict:
    """从已算出的派生确定性拼决策摘要(LLM 缺失时兜底)。缺数据 → 证据不足 + low。"""
    a = (schema.get("feature_tree") or {}).get("analysis") or {}
    arch = (a.get("archetypes") or {}).get(target)
    moats = a.get("moat_candidates") or []
    pm = schema.get("pricing_model") or {}
    price_arch = next((p.get("pricing_engine", {}).get("archetype")
                       for p in pm.get("products") or [] if p.get("name") == target), None)

    def item(answer, conf, refs=None):
        return {"answer": answer or "证据不足", "confidence": conf if answer else "low",
                "refs": refs or []}

    moat_ans = (f"{moats[0]['name']} 为护城河候选" if moats else None)
    moat_conf = moats[0]["confidence"] if moats else "low"
    return {
        "why_success": item(f"{target} 形态为{arch}" if arch else None, "low"),
        "how_monetize": item(f"定价范式 {price_arch}" if price_arch else None,
                             "medium" if price_arch else "low"),
        "moat": item(moat_ans, moat_conf),
        "what_to_learn": item(None, "low"),     # 由 recommendations(Learn) 覆盖,缺则证据不足
        "what_to_avoid": item(None, "low"),
    }
```

并在 derivations 装配末尾（recommendations 产出后）调用兜底：
```python
        if not derivations.get("decision_summary"):
            derivations["decision_summary"] = _fallback_decision_summary(
                {**facts, **derivations}, target=meta.get("target_product") or "")
```

- [ ] **Step 4: 跑测试确认 pass**

Run: `python -m pytest tests/test_writer_decision_report.py::FallbackDecisionSummaryTest -v`
Expected: PASS

- [ ] **Step 5: commit**

```bash
git add src/analyzer.py tests/test_writer_decision_report.py
git commit -m "feat(analyzer): decision_summary 确定性兜底(从派生拼五问 + confidence)"
```

---

### 🚦 Phase 5 验收关卡（用户测试点）

1. **全量测试**：`python -m pytest tests/ -v`（全绿）
2. **端到端跑一次** + 通读报告：
   - §0 决策摘要五问有答案、带 confidence、能追到 chip；
   - §12 建议确实分 Learn/Avoid/Attack，每条带对标对象+优先级+风险；
   - Learn 是否对得上 §7 的 winner，Attack 是否对得上 §9 的 whitespace（逻辑闭环）。
3. **触发 reviewer 校验**：故意造一条 depth_score 无 refs 的数据，确认 R11 打 error。

---

# Phase 6 · 端到端集成 + 总验收

### Task 21: 端到端 golden + 全量回归

**Files:**
- Test: `tests/test_writer_decision_report.py`（加一条 e2e：mock schema → writer_node → 断言四层全章节都在）
- 运行：全量测试 + 一次真实分析

**Interfaces:**
- Consumes: 全链路

- [ ] **Step 1: 写 e2e golden 测试**

```python
# 追加到 tests/test_writer_decision_report.py
class EndToEndReportTest(unittest.TestCase):
    def test_all_sections_present(self):
        # 复用 FourLayerOrderTest 的 state,断言全部章节标题都出现
        state = FourLayerOrderTest().__class__  # 见上,直接构造同 state
        # （实现时把 state 抽成模块级 fixture _DEMO_STATE 复用）
        from src.writer import writer_node
        out = writer_node(_DEMO_STATE)["report_draft"]
        for title in ["决策摘要", "竞品", "功能", "定价", "用户画像", "技术能力",
                      "定位", "功能覆盖与差距", "护城河", "商业模式逻辑", "SWOT",
                      "优先级建议", "口径锁定"]:
            self.assertIn(title, out)
```

> 实现注：把 `FourLayerOrderTest` 里的 state 抽成模块级 `_DEMO_STATE` 常量供两个测试复用（DRY）。

- [ ] **Step 2: 跑全量测试**

Run: `python -m pytest tests/ -v`
Expected: PASS（全部）。若有既有测试因章节重排/schema 变化而失败，逐一更新断言（不是删测试，是对齐新结构）。

- [ ] **Step 3: 真实端到端**

Run: `python -m src.graph --domain jimeng`
Expected: 报告产出，status=passed/degraded（非 failed）；人工通读四层完整。

- [ ] **Step 4: commit**

```bash
git add tests/test_writer_decision_report.py
git commit -m "test: 决策报告四层端到端 golden + 全量回归对齐"
```

---

### 🚦 Phase 6 总验收关卡（用户最终测试点）

逐项核对 spec：

| spec 要求 | 验收方式 |
|---|---|
| 功能树加权三层 + 叶子新字段 | 看 schema `feature_tree.tree` |
| 覆盖率双数 + unknown 排除 | §7 渲染 + `test_feature_model` 覆盖率用例 |
| winner 保守(缺深度→tie) | §7 渲染 + R13 校验 |
| 样本内差异点(无「独占」) | §7 + R12 校验 |
| 护城河叠加难复制因素 | §9 + `MoatTest` |
| 蓝海=高价值未被低门槛解决 | §9 + `WhitespaceTest` |
| 报告四层物理重排 | `FourLayerOrderTest` + 通读 |
| §0 决策摘要带 confidence | §0 渲染 + `DecisionSummaryTest` |
| §3 去 archetype + 顶表 bug 修 | `PricingTopTableTest` + 通读 |
| §5 技术能力去重 | §5 渲染 |
| §10 承接 pricing archetype | `TechAndBizTest` |
| §13 口径锁定表九项 | `InsightsAndLockTest` |
| 建议 Learn/Avoid/Attack | `RecommendationsTriBlockTest` |
| depth 编分校验 | R11 + `DepthHonestyTest` |

---

## Self-Review 记录

- **Spec 覆盖**：Part A（A.1 权重→Task1；A.2 叶子→Task10；A.3 派生→Task2-7）/ Part B（§0→Task12/20；§3→Task14；§5→Task13；§7→Task15；§9→Task16；§10→Task13；§12→Task19；§13→Task16；四层重排→Task17）/ Part C（feature_model→Task2-7；feature_weights→Task1；analyzer→Task8-11/19-20；writer→Task12-17/19；reviewer→Task18）全部有对应任务。`feature_compare.py`（Part C 标「可选」）按 YAGNI 未拆独立模块——跨产品聚合已由 `compute_feature_analysis` 一次性产出，不需要。
- **类型一致**：`compute_feature_analysis` 返回 schema 在 Task7 定义，Task9/15/16/20 消费字段名一致（coverage/winners/differentiation_matrix/archetypes/moat_candidates/whitespace）。叶子字段 support_status/depth_score/evidence_level/differentiator/source_refs 全链路一致。
- **占位符**：无 TBD/TODO；每个代码步给了完整代码。
- **Part E 待确认（实现前需用户拍板，不阻断 Phase 1-4）**：(1) feature_weights 初值现为手填 config（Task1），是否接 LLM 提案后置；(2) §5 技术指标是否按 domains.yaml 分行业——当前 `_TECH_FIELDS` 写死 4 项，按需再 config 化。
